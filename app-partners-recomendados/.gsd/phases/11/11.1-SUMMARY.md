# Summary 11.1: Backend Domain Models & Query Selectors

## Execution Status
Completed: 2026-09-18T13:26:00-03:00
Wave: 1

## Deliverables
1. **`src/api/admin-intelligence.js`**: Módulo de domínio puro (/backend-architect) sem dependências externas nem I/O:
   - `searchCatalogProducts`: Busca rápida case-insensitive por nome, ID, categoria, cor em produtos publicados com ordenação por relevância.
   - `inspectProductRecommendations`: Inspeção determinística de vitrines via `recommendForProduct`, enriquecendo os itens com `matchReason` (`proven_look`, `same_fabric`, `color_stock`), `weight` (0, 1, 2) e detalhamento de estoque por variante.
   - `getCatalogHealth`: Agregação de cobertura para as 11 categorias da Talgui, diagnóstico de peças sem vitrine e cálculo de margem do disjuntor de segurança (churn vs 30%, apagão vs 10%).
   - `getCoPurchasePairsReport`: Enriquecimento e paginação dos 1.373 pares de co-compra reais (`co_purchase_pairs`), cruzando status de publicação e estoque ativo em ambas as peças.
2. **`src/api/admin-intelligence.test.js`**: 13 testes unitários com Vitest passando com 100% de sucesso.
3. **`web/scripts/prepare-admin-api.mjs`**: Atualizado para vendorizar `admin-intelligence.js` e `recommendation-engine.js` para `web/api/_lib/` com caminhos relativos resolvidos.
4. **`src/db/catalog-store.js`**: Enriquecido com `canonical_url`, `category_raw` e exportação nativa `getAllCoPurchasePairs()`.

## Verification
- `npm test`: 341 testes passando em 26 arquivos de teste.
- `node -c web/api/_lib/admin-intelligence.js`: Sintaxe validada.
