---
phase: 11
plan: 6
wave: 6
---

# Plan 11.6: Verificação Empírica, Screenshots no Browser & Documentação

## Objective
Validar ponta a ponta todas as abas e funcionalidades implementadas no Painel do App Recom, capturando evidências empíricas (testes automatizados, build, lint e screenshots no browser real com a ferramenta browser_subagent), atualizando o script de sincronização Vercel e consolidando a documentação de encerramento da fase.

## Context
- `.gsd/phases/11/11.1-PLAN.md` a `11.5-PLAN.md`
- `web/package.json`
- `PROJECT_RULES.md`

## Tasks

<task type="auto">
  <name>Executar bateria de testes automatizados, build e lint</name>
  <files>web/package.json, src/api/admin-intelligence.test.js</files>
  <action>
    1. Executar os testes unitários do backend e domínio: `npm run test` ou comando equivalente do Vitest/Node.
    2. Executar sincronização da Vercel: `npm run sync:admin-api` dentro de `web/`.
    3. Executar o linter de frontend: `npm run lint` em `web/` com oxlint.
    4. Executar o build de produção do Vite: `npm run build` em `web/`.
    Garantir que não há erros de tipagem, dependências faltando ou falhas de bundle.
  </action>
  <verify>npm run build na pasta web && npm run lint na pasta web</verify>
  <done>Build e lint finalizados com zero erros</done>
</task>

<task type="auto">
  <name>Capturar screenshots empíricos de todas as abas no browser real</name>
  <files>web/dist/</files>
  <action>
    1. Iniciar servidor local com a versão construída do painel.
    2. Usar o subagente de browser para navegar em cada aba e capturar screenshots em alta resolução (1920x1080):
       - Aba Visão Geral (Overview) com cards de indicadores e telemetria do cron.
       - Aba Inspetor de Vitrines com busca de um produto real da loja (ex: Vestido Elaine ou Saia Eloá) e exibição das recomendações com suas badges.
       - Aba Looks Comprovados com a tabela de pares e filtros.
       - Aba Saúde do Catálogo com a matriz das 11 categorias e disjuntor.
       - Aba Vínculo Manual com o scaffolding estruturado para o Claude Code.
    3. Salvar os screenshots no diretório de artefatos.
  </action>
  <verify>Verificar existência dos arquivos de screenshot e status da captura</verify>
  <done>Screenshots de alta resolução capturados comprovando o funcionamento visual e interativo</done>
</task>

<task type="auto">
  <name>Gerar relatório de validação e atualizar estado no GSD</name>
  <files>.gsd/phases/11/VERIFICATION.md, .gsd/ROADMAP.md, .gsd/STATE.md</files>
  <action>
    1. Criar `.gsd/phases/11/VERIFICATION.md` documentando todos os resultados de testes, comandos executados e links dos screenshots capturados.
    2. Atualizar `.gsd/ROADMAP.md` marcando a Phase 11 como concluída.
    3. Atualizar `.gsd/STATE.md` com o resumo da entrega e handoff para o Claude Code (vínculo manual).
  </action>
  <verify>test -f .gsd/phases/11/VERIFICATION.md</verify>
  <done>VERIFICATION.md gerado e ROADMAP.md/STATE.md atualizados</done>
</task>

## Success Criteria
- [ ] Build e Lint de produção passando com zero erros
- [ ] Screenshots de todas as abas capturados no browser real
- [ ] Relatório empírico VERIFICATION.md consolidado
- [ ] Handoff para o Claude Code formalizado
