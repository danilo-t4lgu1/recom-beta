---
context: default
phase: none (trabalho fora dos planos GSD)
task: n/a
total_tasks: n/a
status: paused
last_updated: 2026-08-10T15:00:00Z
---

# Handoff — Refinamento do Painel Administrativo do Recom (4 Cards) + fix do App Center

> Pausado para migração de conta. Sem bloqueio técnico — é trabalho de design/implementação
> incremental, retomável a qualquer momento a partir dos itens em `<remaining_work>`.

## Contexto: por que esse trabalho existe

Continuação do painel administrativo do Recom (`app-partners-recomendados/web/`, deployado em
`https://recom-admin-talgui.vercel.app`) e do hub Talgui App Center
(`https://talgui-app-center.vercel.app`), ambos concluídos numa sessão anterior (ver D-71 a
D-74 em `PROJECT.md`). Nesta sessão, o usuário **usou o painel de verdade em produção** e trouxe
ressalvas concretas sobre os 4 cards existentes — este handoff cobre essa rodada de refinamento.

Repos envolvidos (os mesmos de sempre):
- `C:\Users\danil\Desktop\Recomendados Talgui` — projeto principal (GSD), remote
  `https://github.com/danilo-t4lgu1/recom-beta`. Onde vive o motor + o painel do Recom.
- `C:\Users\danil\Desktop\Talgui App Center` — hub separado, remote
  `https://github.com/danilo-t4lgu1/talgui-app-center`.

<current_state>
**Bug do App Center: RESOLVIDO e verificado em produção.** O card do Recom linkava para
`http://localhost:5174` (env var `VITE_RECOM_ADMIN_URL` nunca foi criada na Vercel). Corrigido
o fallback direto no código para `https://recom-admin-talgui.vercel.app`
(`src/data/apps.js`, commit `ad856ee`, já deployado — confirmei o bundle publicado contém a
URL certa, sem nenhum resquício de localhost).

**Persistência do Card 4 (Cron Diário): IMPLEMENTADA e no ar**, mas com dado histórico
começando só a partir de 2026-08-11 (ver `<data_gaps>` abaixo). Nova tabela
`daily_recompute_log` (commit `74d22cd`, já em produção) grava, a cada execução do job diário,
se a etapa de RECOMPUTE/ESCRITA teve sucesso (`status='ok'`) ou falhou (`status='error'`,
com `reason`) — distinta de `ingestion_runs.status` (que só cobre a etapa de INGESTÃO). Também
corrigi o workflow `.github/workflows/daily-recompute.yml`: os passos de sincronizar o catálogo
vendorizado e o commit-back agora rodam com `if: always()`, porque sem isso o GitHub Actions
pulava os dois quando o passo de ingestão falhava com exceção — o registro de erro ficaria
preso só no runner efêmero, nunca chegando ao git/painel.

**Redesenho dos 4 cards do painel: DISCUTIDO E ESPECIFICADO, NADA IMPLEMENTADO AINDA** (exceto
a persistência acima, que é pré-requisito de dado para o Card 4). Ver `<remaining_work>` para
o que falta, card por card.
</current_state>

<completed_work>

**Fix do App Center (`Talgui App Center` repo):**
- `src/data/apps.js`: fallback do card Recom trocado de `http://localhost:5174` para
  `https://recom-admin-talgui.vercel.app` (commit `ad856ee`, pushado, auto-deploy `Ready` em
  ~8s, verificado via curl no bundle JS publicado).

**Persistência do Card 4 (`app-partners-recomendados` repo, dentro do `recom-beta`):**
- `src/db/schema.sql`: nova tabela `daily_recompute_log` (id, run_id NULLABLE, started_at,
  finished_at, status 'ok'|'error', reason, alterados, zerados, novos, dry_run).
- `src/db/catalog-store.js`: `insertDailyRecomputeLog`, `getDailyRecomputeLogSince`,
  `listDailyRecomputeLog` (mesmo padrão de `insertWriteLog`/`listWriteLog`).
- `scripts/run-daily-job.js`: grava uma linha em CADA ponto de saída — sucesso normal (`ok`),
  aborto pela Defesa 1 (`error`), aborto pelo disjuntor (`error`), e exceção não tratada no
  catch-all do CLI (`error`, `runId: null` — cobre o cenário do incidente real de 2026-07-23,
  502 da Cloudflare, que falhou ANTES de criar uma linha em `ingestion_runs`).
- `.github/workflows/daily-recompute.yml`: `if: always()` nos passos de sincronizar catálogo
  vendorizado e commit-back, para que um erro catastrófico ainda assim chegue ao git.
- Commit `74d22cd`, pushado. 275/275 testes passando (11 testes novos entre
  `catalog-store.test.js`/`run-daily-job.test.js`).

**Investigação/respostas dadas ao usuário nesta sessão (sem mudança de código, só achados):**
- `MIN_SIZES_IN_STOCK` confirmado como **1** (não revertido para o default 3) — descoberto
  via query direta em `catalog_snapshots` (produtos com `sizes_in_stock_count=1` marcados
  `has_available_grade=1`), sem precisar de acesso ao GitHub Actions. Método documentado no
  chat, útil para o usuário reproduzir sozinho no futuro.
- Confirmado no código (`src/api/recommendations.js` + `storefront-script/main.js`) que
  **produtos recomendados sem estoque aparecem sim no carrossel real** — nenhum filtro de
  estoque existe nessas duas camadas, só chips visuais "off" por tamanho. Valida a proposta de
  redesenhar o Card 2.
- **Achado à parte, não urgente:** `storefront-script/main-partners.js` (citado o dia inteiro
  em sessões anteriores) não existe mais na pasta — parece ter sido consolidado dentro de
  `main.js` em outra sessão (entre 06/08 e 07/08, mesmo tamanho de arquivo). O script
  publicado/ativo hoje é `main.js`. Não afeta nada, só vale confirmar com o usuário quando
  puder.
</completed_work>

<remaining_work>

Card a card, o que falta implementar (nenhum código escrito ainda para os 4 itens abaixo):

### Card 1 — Produtos Relacionados (sem mudança pendente)
Só esclarecimento dado ao usuário: "1017" é contagem de PRODUTOS (não SKUs) com pelo menos
1 recomendação atualmente gravada no `write_log` (estado real na loja, não recompute). Nenhuma
mudança de código pedida aqui.

### Card 2 — Estoque dos Relacionados (DECISÃO DO USUÁRIO AINDA PENDENTE)
Proposta feita (aceita implicitamente, mas sem confirmação explícita de "pode implementar"):
trocar a exibição de unidades totais (`18.070un`) por **"Recomendados sem estoque: N de X"**
(usar `zeroStockCount` já calculado em `admin-dashboard.js`, é só mudar o que o front exibe —
o backend já devolve esse número). Perguntar ao usuário se pode seguir com essa troca antes de
implementar.

### Card 3 — Tecido Preenchido em Tags (SPEC FECHADA, nada implementado)
- Card muda para framing de gap: **"1906 SKUs sem Tag [TECIDO] Preenchido"** (inverter — hoje
  mostra os preenchidos) + botão "Ver Detalhes".
- Expansão do PRÓPRIO card (não página nova) mostrando por grupo de tecido (Alfaiataria,
  Malha, Crepe, Tule, Couro PU): quantidade de SKUs associados + quantidade sem tag.
- Export Excel com EXATAMENTE estas colunas (nomes confirmados no arquivo de exemplo do
  usuário, não no resumo em texto): `ID_Nuvem | Nome | SKU | Estoque | Tag | Grupo_De_Tecidos`
  — `Tag` é SIM/NÃO, `Grupo_De_Tecidos` fica vazio quando `Tag=NÃO`.
- **Arquivo de exemplo do usuário:** `C:\Users\danil\Desktop\catalogo_tags_tecidos_10_08.xlsx`
  (2 linhas de exemplo, já lido e confirmado nesta sessão).
- **Pré-requisito técnico:** `getLatestSnapshotProducts()` (`src/db/catalog-store.js`) hoje
  NÃO devolve `sku` por variante (só `variantId`/`sizeValue`/`stockTotal`), embora a coluna
  `sku` já exista na tabela `variants`. Extensão aditiva simples, mesmo padrão do D-73 anterior
  (extensão de `getLastSuccessfulIngestionRunSummary`) — não deve quebrar nada, mas precisa ser
  feita antes do export funcionar.
- Qual SKU usar quando o produto tem múltiplas variantes/tamanhos: usuário disse "qualquer SKU
  relacionado a esse ID" — pegar a primeira variante não-nula é suficiente.

### Card 4 — Cron Diário (persistência PRONTA, front-end/export NÃO implementados)
- Card muda para: **"Cron Diário - Atualizado"** (ícone de check) + Data + Horário do último
  run.
- Clicar expande um segundo card com log dos **últimos 7 dias** + status de cada dia.
- Botão de exportar planilha com histórico **desde o primeiro cron em produção real**
  (2026-07-17, `ingestion_runs.id=1`) até hoje, colunas: `Data | Hora | Status | Catalogo |
  Relacionados | Daily_Recompute`.
- **Arquivo de exemplo do usuário:** `C:\Users\danil\Desktop\log_cron.xlsx` (3 linhas de
  exemplo, já lido e confirmado nesta sessão — `Status` usa "ATUALIZADO"/"NÃO ATUALIZADO",
  `Daily_Recompute` usa "OK"/"ERROR").
- Semântica confirmada pelo usuário: `Status` = a ingestão rodou hoje, independente do
  resultado do recompute. `Daily_Recompute` = a etapa de escrita/recompute teve sucesso —
  pode dar ERROR mesmo com `Status` OK (ingestão certa, escrita com problema). São 2 sinais
  independentes de propósito.
- Falta: função de leitura que faça o JOIN entre `ingestion_runs` (Data/Hora/Catalogo/Status)
  e `daily_recompute_log` (Daily_Recompute) por dia; wiring no `admin-dashboard.js`/
  `admin-server.js`; UI da expansão de 7 dias; geração do XLSX de export (endpoint novo,
  provavelmente `GET /api/dashboard/cron-log.xlsx` ou similar — formato a definir).

<data_gaps>
Duas limitações REAIS de dado, já explicadas ao usuário, que quem implementar o export
precisa saber:

1. **`Daily_Recompute` só existe a partir de 2026-08-11** (a persistência foi ligada em
   2026-08-10, DEPOIS do cron de hoje já ter rodado). Dias anteriores a essa data ficam sem
   esse dado — não há como reconstruir retroativamente (o resumo só ia pro Slack antes).
   `Status`/`Catalogo`/`Relacionados` SÃO reconstruíveis desde 2026-07-17 via
   `ingestion_runs`/`write_log` histórico.
2. **O incidente de 2026-07-23 (502 da Cloudflare) não deixou NENHUMA linha em
   `ingestion_runs`** — o run falhou antes de qualquer commit chegar ao git. Reconstruir
   "Status: NÃO ATUALIZADO" para esse dia especificamente exige cruzar com a API pública do
   GitHub Actions (`GET /repos/danilo-t4lgu1/recom-beta/actions/workflows/daily-recompute.yml/runs`
   — funciona SEM autenticação, confirmado nesta e em sessões anteriores), não só o banco local.
   Dias a partir de 2026-08-10 já não têm mais esse problema (a persistência nova cobre esse
   caminho com `runId: null`).
</data_gaps>

</remaining_work>

<decisions_made>
- Card 2: recomendação dada (trocar unidades por "sem estoque: N de X"), mas falta o usuário
  confirmar antes de implementar — não presumir "sim" e já mexer no código sem essa confirmação.
- Card 3/Card 4: nomes de coluna do EXPORT seguem os arquivos de exemplo (`.xlsx`) literalmente,
  não o resumo em texto do usuário (que usava nomes ligeiramente diferentes, ex: "TECIDOS" no
  texto vira `Grupo_De_Tecidos` no arquivo real).
- `daily_recompute_log` desenhado com `run_id` NULLABLE de propósito, para nunca deixar um dia
  sem registro só porque a ingestão falhou cedo demais para criar uma `ingestion_runs` row.
</decisions_made>

<blockers>
Nenhum blocker técnico. O que falta é: (1) confirmação do usuário sobre o Card 2, e (2)
implementação (backend + front + export Excel) dos Cards 3 e 4, já com spec fechada.
</blockers>

## Human actions pending

Nenhuma ação humana bloqueante. Os dois arquivos de exemplo (`catalogo_tags_tecidos_10_08.xlsx`,
`log_cron.xlsx`) continuam em `C:\Users\danil\Desktop\` — não foram movidos nem apagados.

## Infrastructure State

- `recom-admin` (Vercel): no ar, protegido, dado real (confirmado hoje: 1017/3222 relacionados,
  18.070un em estoque nos relacionados, 1316/3222 com tag de tecido, cron rodando diariamente).
- `talgui-app-center` (Vercel): no ar, protegido, card do Recom agora linkando certo para
  produção.
- `daily_recompute_log`: schema e persistência em produção desde o commit `74d22cd`
  (2026-08-10) — primeira linha real esperada no cron de 2026-08-11.

<next_action>
Ao retomar: perguntar ao usuário se pode seguir com a troca do Card 2 (unidades → "sem
estoque: N de X"). Em paralelo ou depois, implementar Card 3 (extensão de `sku` em
`getLatestSnapshotProducts`, agregação por `Grupo_De_Tecidos`, endpoint de export XLSX) e
Card 4 (join `ingestion_runs` + `daily_recompute_log`, UI de expansão de 7 dias, endpoint de
export XLSX com histórico completo desde 2026-07-17). Os dois arquivos de exemplo do usuário
em `C:\Users\danil\Desktop\` são a fonte de verdade para o formato exato das colunas — reler
antes de implementar, não confiar só neste resumo em texto.
</next_action>
