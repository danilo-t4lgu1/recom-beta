---
context: default
phase: none (trabalho fora dos planos GSD + acompanhamento operacional)
task: n/a
total_tasks: n/a
status: paused
last_updated: 2026-08-12T13:15:00Z
---

# Handoff — Estado geral do projeto para retomada em conta pessoal

> Este handoff cobre o projeto inteiro, não só a última tarefa — foi escrito para uma
> retomada real em outra conta (sessão anterior encerrando). Leia `<priority>` primeiro.

<priority>
## 🔴 Prioridade #1 — Disjuntor bloqueando a escrita automática há 2 dias

O job diário (`run-daily-job.js`) tentou escrever e foi **abortado pelo disjuntor (D-63)**
em 2 das últimas 3 execuções:

- **2026-08-11 06:46 UTC**: `churn 65.9% > 30%` — abortado
- **2026-08-11 15:03 UTC** (2ª tentativa no mesmo dia): passou, gravou 538 alterados/168
  zerados/56 novos
- **2026-08-12 07:07 UTC (hoje)**: `apagão 15.8% > 10%` — abortado

**Diagnóstico já feito nesta sessão** (script de investigação, não commitado — ver
`<how_to_reproduce>`): dos 169 produtos que ficariam sem vitrine hoje:

- **158 (93%)** — genuinamente sem estoque (`hasAvailableGrade=false`). Esperado durante o
  período promocional (`MIN_SIZES_IN_STOCK=1` ainda ativo). **Sozinhos já somam 14,7% —
  acima do limiar de 10% mesmo sem os outros 15 casos.** Ou seja, mesmo corrigindo a
  categorização abaixo, o disjuntor continuaria bloqueado hoje.
- **15 (7%)** — produtos sem NENHUMA categoria de taxonomia real, só a coleção "Últimas
  Oportunidades" (mesmo bug de categorização do "EDIT", já corrigido antes para outros 16
  produtos). Lista completa:
  - **13× "Regata"** (Mireille×5 cores, Úrsula×2, Ludimila×3, Ariela×3) — usuário confirmou
    nesta sessão: deveriam entrar em **"Blusas"** (grupo Partes de Cima). IDs: `322821097,
    322821103, 322821107, 322821114, 322821121, 322823004, 322823014, 322823171, 322823182,
    322823483, 322823494, 322823503, 322881522`
  - **1× "Casaco Raquel Alfaiataria Lisa Bege"** (`346662640`) — deveria entrar em
    **"Blazers e Jaquetas"** (mesmo grupo).
  - **1× "Gloss Talgui"** (`339747108`) — **NÃO é roupa** (produto de beleza/cosmético
    parado na mesma coleção). Não é gap de categoria — provavelmente não deveria nem estar
    no motor. Decisão de negócio separada (mesma classe do "Lenços", ver `<pendencias>`).

**Decisão do usuário nesta sessão:** não fazer override manual agora — deixar a decisão
para a próxima sessão. A vitrine fica com dado desatualizado (não incorreto — o disjuntor
só bloqueia, nunca corrompe nada) até alguém decidir.

**O que a próxima sessão precisa decidir:**
1. Corrigir as categorias dos 14 produtos de roupa no admin da Nuvemshop (mesmo processo já
   feito para o "EDIT") — resolve o gap de dado, mas NÃO desbloqueia a escrita sozinho.
2. Decidir sobre os 158 estoques zerados: é esperado (aceitar o apagão, rodar
   `workflow_dispatch` com override manual do disjuntor um vez) ou o limiar de 10% precisa
   ser revisto enquanto durar a promoção (`blackoutMax` em `circuit-breaker.js`)?
3. Decidir o que fazer com "Gloss Talgui" (fora do motor, ou remover da coleção?).

<how_to_reproduce>
Script usado para o diagnóstico (não ficou salvo, recriar se precisar):
```js
import { getLatestSnapshotProducts, getLastWrittenValuesForAllProducts } from './src/db/catalog-store.js';
import { recommendForProduct } from './src/recommendation/recommendation-engine.js';
// computa recommendForProduct para cada fonte elegível, compara contra
// getLastWrittenValuesForAllProducts() (baseline), classifica quem ficaria vazio
// (apagão) e por quê (hasAvailableGrade / productGroupCanonical / etc).
```
Ou simplesmente rodar `node scripts/engine-certification-report.js` (commitado nesta sessão,
ver `<completed_this_session>`) — cobre um diagnóstico relacionado (write_log vs recompute
fresco), embora não replique o cálculo exato do disjuntor.
</how_to_reproduce>
</priority>

<completed_this_session>
Tudo abaixo já está commitado, pushado e verificado em produção — não precisa de retrabalho.

**Refinamento do Painel Administrativo (4 cards)** — commit `6b383ff`:
- Card 2: "Recomendados sem estoque: N de X" (era unidades totais).
- Card 3: "N SKUs sem Tag [TECIDO]" + expansão por Grupo_De_Tecidos + export Excel.
- Card 4: "Cron Diário - Atualizado" + data/hora + expansão 7 dias + export Excel com
  histórico completo desde 2026-07-17.
- Export via `exceljs` (não `xlsx`/SheetJS — 2 vulnerabilidades HIGH sem correção).
- 293/293 testes passando. Verificado em produção via curl (JSON + xlsx).

**Persistência do Card 4** (`daily_recompute_log`) — commit `74d22cd`:
- Já está gerando dado real (ver os 3 registros do disjuntor em `<priority>` acima — a
  própria feature capturou o problema que estamos documentando agora).

**Fix do Talgui App Center** — commit `ad856ee` (repo separado):
- Card do Recom linkava para `localhost:5174`, corrigido para a URL de produção.

**Ferramenta de certificação do motor** (`engine-certification-report.js`) — commit `d3af5d8`:
- Diagnóstico read-only comparando `write_log` (o que está gravado) vs. recompute fresco.
- Estava sem commit desde bem no início da sessão — commitado agora antes de fechar.

**`.planning/` versionado no git** — commit `ffac6e0`:
- Antes só `PROJECT.md` estava no git; agora as 11 fases, decisões, roadmap, tudo tem
  backup real. 2 senhas em texto puro foram encontradas e redigidas antes de versionar
  (repo `recom-beta` é público).

**Fase 9 (Dashboard GA4) formalmente ADIADA** — commit `bd024d9`:
- Decisão do usuário: prioridade é o ecossistema de apps Talgui, não atribuição de métricas
  GA4 ainda (tempo de atividade do bloco Recomendados curto demais). Pesquisa preservada em
  `09-RESEARCH.md`/`09-CONTEXT.md` para quando for retomada.
</completed_this_session>

<pendencias>
## Outras pendências conhecidas do projeto (não criadas nesta sessão, mas ainda abertas)

- **"Lenços" fora do fechamento D-26** (STATE.md, adiada pelo usuário em 2026-07-23): mesmo
  padrão do "Gloss Talgui" acima — tipo de produto fora dos 3 grupos fechados
  (Look Inteiro/Partes de Cima/Partes de Baixo). Precisa de decisão de negócio: entra em
  algum grupo como acessório, ou fica fora do motor?
- **`MIN_SIZES_IN_STOCK=1`** (repository variable no GitHub Actions): afrouxado
  intencionalmente durante a Ação Promocional. **Reverter para o default (apagar a
  variável, ou setar 3) quando o estoque normalizar** — hoje ainda está gerando o volume
  alto de "grade disponível" que contribui para o churn observado no disjuntor.
- **Fase 10 (Migração NubeSDK) — prazo regulatório se aproximando:** bloqueio de novas
  instalações do Script legado em **30/08/2026** (faltam ~18 dias a partir de hoje,
  2026-08-12), remoção progressiva a partir de 30/10/2026. Fase 10 no roadmap ainda com
  **0 plans** (nunca foi planejada/iniciada, `/gsd-plan-phase 10` nunca rodou). Vale
  priorizar antes do prazo.
- **Fase 7, plano 07-08 formalmente não executado** (`[ ] 07-08-PLAN.md` no ROADMAP.md):
  "Rollout supervisionado do 1º run" — na PRÁTICA já aconteceu (escritas reais em produção
  confirmadas o tempo todo nesta sessão), só não foi formalmente fechado via
  `/gsd-execute-phase`. Housekeeping, não bloqueia nada.
- **`storefront-script/main-partners.js` não existe mais** (achado em sessão anterior, ainda
  não confirmado com o usuário): parece ter sido consolidado dentro de `main.js` em algum
  momento entre 2026-08-06/07. O script ativo hoje é `main.js`. Não afeta funcionamento, só
  nomenclatura desatualizada em documentação antiga.
</pendencias>

## Infrastructure State

- `recom-admin` (Vercel): no ar, protegido, dado real, painel com os 4 cards + 2 exports
  Excel funcionando.
- `talgui-app-center` (Vercel): no ar, protegido, card do Recom linkando certo.
- `recom-beta` (GitHub, repo principal): público, `.planning/` versionado, branch `master`
  sincronizada com origin no momento deste handoff.
- Cron diário: rodando todo dia (confirmado via `ingestion_runs` até run #28, 2026-08-12),
  mas a ETAPA DE ESCRITA está bloqueada pelo disjuntor desde ontem de manhã (ver
  `<priority>`) — a INGESTÃO continua funcionando normalmente, só a escrita de
  recomendações que está pausada.

<next_action>
Ao retomar: resolver `<priority>` primeiro (decisão sobre o disjuntor bloqueado + correção
de categoria dos 14 produtos de roupa no admin da Nuvemshop). Depois, considerar priorizar
a Fase 10 (NubeSDK) dado o prazo de 30/08/2026 se aproximando.
</next_action>
