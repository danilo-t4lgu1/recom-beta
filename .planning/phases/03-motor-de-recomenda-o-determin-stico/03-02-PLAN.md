---
phase: 03-motor-de-recomenda-o-determin-stico
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app-partners-recomendados/src/db/catalog-store.js
  - app-partners-recomendados/src/recommendation/recommend-cli.js
autonomous: true
requirements: [RULE-01, RULE-02]
must_haves:
  truths:
    - "getLatestSnapshotProducts() retorna um array de entradas exatamente no shape CatalogProductEntry que recommendForProduct consome (productId, name, colorValue, fabricTagCanonical, hasAvailableGrade, variants[])"
    - "A leitura usa apenas o último run de ingestão com status success e as variantes vistas nesse mesmo run — nunca mistura runs nem inclui variantes obsoletas"
    - "colorValue vem de variants.color_value (granularidade correta per IN-03), não de catalog_snapshots.color_value"
    - "node src/recommendation/recommend-cli.js <productId> imprime JSON idêntico em duas execuções consecutivas, sem nenhuma chamada de rede (RULE-02 / Success Criteria #3 e #4 sobre o snapshot real)"
  artifacts:
    - path: "app-partners-recomendados/src/db/catalog-store.js"
      provides: "getLatestSnapshotProducts() — leitura do snapshot do último run bem-sucedido no shape do motor, via prepared statements, sem expor o objeto db"
      contains: "export function getLatestSnapshotProducts"
    - path: "app-partners-recomendados/src/recommendation/recommend-cli.js"
      provides: "CLI de preview somente-leitura: node recommend-cli.js <productId> => JSON das recomendações do motor sobre o snapshot real"
      contains: "recommendForProduct"
  key_links:
    - from: "app-partners-recomendados/src/recommendation/recommend-cli.js"
      to: "app-partners-recomendados/src/recommendation/recommendation-engine.js"
      via: "import de recommendForProduct — o CLI nunca reimplementa elegibilidade/ranking"
      pattern: "recommendForProduct"
    - from: "app-partners-recomendados/src/recommendation/recommend-cli.js"
      to: "app-partners-recomendados/src/db/catalog-store.js"
      via: "import de getLatestSnapshotProducts — único acesso a dados do CLI"
      pattern: "getLatestSnapshotProducts"
    - from: "app-partners-recomendados/src/db/catalog-store.js"
      to: "app-partners-recomendados/src/db/schema.sql"
      via: "prepared statements sobre ingestion_runs, catalog_snapshots, products e variants"
      pattern: "catalog_snapshots"
---

<objective>
Completar a fatia vertical da Fase 3 ligando o motor puro (plano 03-01) ao snapshot real persistido pela Fase 2: uma nova função de leitura `getLatestSnapshotProducts()` em `catalog-store.js` que materializa o shape `CatalogProductEntry` a partir do último run de ingestão bem-sucedido, e um CLI de preview somente-leitura (`recommend-cli.js`) que permite rodar o motor produto a produto (D-17) contra `data/catalog.db` sem nenhuma rede.

Exercício de discretion (registrado per 03-CONTEXT.md / IN-03): a cor representativa do produto vem de `variants.color_value` (granularidade correta por variante), NÃO de `catalog_snapshots.color_value` (sabidamente derivada só da primeira variante da API). Hoje 0 produtos multi-cor existem, então os valores coincidem — mas a fonte escolhida é a robusta.

Comportamento real esperado HOJE: como 0/645 produtos têm `fabric_tag_canonical` preenchido (importação da planilha pendente, D-16), o motor estrito (D-15) retorna `[]` para qualquer produto real — isso é CORRETO, não bug. Quando o usuário importar a planilha, este mesmo CLI passa a devolver recomendações reais sem nenhuma mudança de código.

Purpose: sem esta leitura, a Fase 4 não teria como alimentar o preview com dados reais; o CLI é a primeira capacidade operável de ponta a ponta do motor e a ferramenta de validação manual do usuário na semana da importação da planilha.
Output: `getLatestSnapshotProducts()` exportada em `catalog-store.js` + `src/recommendation/recommend-cli.js` executável e determinístico.
</objective>

## Phase Goal

Dado um snapshot normalizado do catálogo, o sistema calcula automaticamente até 8 produtos recomendados por produto, aplicando simultaneamente mesma cor, mesmo tipo de tecido (padronizado) e estoque disponível obrigatório — de forma 100% determinística, sem IA/ML, auditável e testável isoladamente.

*(Goal do ROADMAP mantido verbatim; não está no formato user-story canônico do modo MVP — discrepância registrada pelo planner, não bloqueia execução.)*

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-motor-de-recomenda-o-determin-stico/03-CONTEXT.md
@.planning/phases/03-motor-de-recomenda-o-determin-stico/03-PATTERNS.md
@.planning/phases/03-motor-de-recomenda-o-determin-stico/03-01-SUMMARY.md
@app-partners-recomendados/src/db/catalog-store.js
@app-partners-recomendados/src/db/schema.sql
@app-partners-recomendados/src/recommendation/recommendation-engine.js
</context>

<interfaces>
<!-- Contratos que o executor usa diretamente — sem exploração de codebase necessária. -->

Do plano 03-01 (já executado — Wave 1):

```javascript
// src/recommendation/recommendation-engine.js
export function recommendForProduct(productId, catalogProducts, { maxRecommendations = MAX_RECOMMENDATIONS } = {})
export const MAX_RECOMMENDATIONS = 8;
```

Shape que `getLatestSnapshotProducts()` DEVE produzir — `CatalogProductEntry` (contrato definido no plano 03-01):

```javascript
{
  productId: string,               // products.id / catalog_snapshots.product_id
  name: string|null,               // products.name
  colorValue: string|null,         // color_value da PRIMEIRA variante na ordem determinística (IN-03)
  fabricTagCanonical: string|null, // catalog_snapshots.fabric_tag_canonical do run selecionado
  hasAvailableGrade: boolean,      // catalog_snapshots.has_available_grade === 1
  variants: [
    { variantId: string, sizeValue: string|null, stockTotal: number }  // variants do mesmo run
  ]
}
```

Colunas disponíveis (schema.sql, já criado na Fase 2): `ingestion_runs(id, status, ...)`; `catalog_snapshots(run_id, product_id, has_available_grade, sizes_in_stock_count, fabric_tag_canonical, color_value, snapshot_at)`; `products(id, name, handle, canonical_url, last_seen_run_id)`; `variants(id, product_id, sku, color_value, size_value, stock_total, last_seen_run_id)`.

Convenções obrigatórias de `catalog-store.js` (header do próprio arquivo): exportar apenas funções nomeadas, NUNCA o objeto `db` cru; prepared statements no topo do módulo com parâmetros nomeados `@param`; nunca concatenar dado de produto em string SQL (T-02-04).
</interfaces>

## Artifacts this phase produces (deste plano)

| Artifact | Type | Provides |
|----------|------|----------|
| `getLatestSnapshotProducts()` (função, `catalog-store.js`) | função exportada nova | Snapshot do último run success no shape CatalogProductEntry consumido pelo motor |
| `selectLatestSuccessfulRun`, `selectSnapshotsForRun`, `selectVariantsForRun` (prepared statements, `catalog-store.js`) | statements internos novos (não exportados) | Queries preparadas da leitura do snapshot |
| `src/recommendation/recommend-cli.js` | arquivo novo (script Node, sem exports) | Preview somente-leitura: `node src/recommendation/recommend-cli.js <productId>` imprime JSON das recomendações |

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: getLatestSnapshotProducts() — snapshot real no shape do motor</name>
  <files>app-partners-recomendados/src/db/catalog-store.js</files>
  <read_first>
    app-partners-recomendados/src/db/catalog-store.js
    app-partners-recomendados/src/db/schema.sql
    .planning/phases/03-motor-de-recomenda-o-determin-stico/03-CONTEXT.md
    .planning/phases/03-motor-de-recomenda-o-determin-stico/03-PATTERNS.md
    app-partners-recomendados/src/recommendation/recommendation-engine.js
  </read_first>
  <action>
    Adicionar a `src/db/catalog-store.js` (arquivo existente — preservar tudo que já existe, incluindo a convenção de nunca exportar o objeto db) três prepared statements no topo do módulo, seguindo o estilo de `selectCanonicalMap`:

    1. `selectLatestSuccessfulRun`: `SELECT id FROM ingestion_runs WHERE status = 'success' ORDER BY id DESC LIMIT 1`
    2. `selectSnapshotsForRun`: join de `catalog_snapshots s` com `products p` em `p.id = s.product_id`, selecionando `s.product_id`, `s.fabric_tag_canonical`, `s.has_available_grade`, `p.name`, filtrando `s.run_id = @runId`, ordenando por `s.product_id` (ordem determinística)
    3. `selectVariantsForRun`: `SELECT id, product_id, color_value, size_value, stock_total FROM variants WHERE last_seen_run_id = @runId ORDER BY product_id, id` — o filtro por `last_seen_run_id` garante que variantes de produtos que saíram do catálogo em runs anteriores não vazem para o snapshot atual (products/variants são upsert de estado mais recente; catalog_snapshots é append-only por run)

    Exportar `export function getLatestSnapshotProducts()`: resolve o run via statement 1 (nenhum run success => retorna `[]`, comportamento documentado no JSDoc, mesmo padrão de `getCanonicalMap` com tabela vazia); agrupa as variantes por `product_id` em um Map; monta e retorna o array de `CatalogProductEntry` exatamente no shape do bloco `<interfaces>`: `productId` = `String(product_id)`, `name`, `fabricTagCanonical` = `fabric_tag_canonical` (null permanece null — D-15 é aplicado pelo motor, não aqui: esta função NÃO filtra elegibilidade, devolve o snapshot completo para o motor decidir), `hasAvailableGrade` = `has_available_grade === 1` (conversão explícita 0/1 => boolean), `variants` = array `{ variantId: String(id), sizeValue: size_value, stockTotal: stock_total }` na ordem do statement 3, e `colorValue` = `color_value` da PRIMEIRA variante do produto nessa ordem determinística (ou null se o produto não tiver variantes).

    JSDoc obrigatório citando: D-17 (shape consumido produto a produto por `recommendForProduct`), IN-03 + Claude's Discretion do 03-CONTEXT.md (cor lida de `variants.color_value`, granularidade por variante, e não de `catalog_snapshots.color_value`, que é derivada só da primeira variante da API e não confiável para multi-cor; hoje 0 produtos multi-cor, valores idênticos na prática), e a semântica de run único (snapshot = último run success, nunca mistura de runs).

    Nenhum parâmetro externo entra em SQL: `@runId` vem do próprio statement 1. Sem timestamps, sem escrita — função de leitura pura sobre o estado do banco.
  </action>
  <acceptance_criteria>
    - `src/db/catalog-store.js` contém `export function getLatestSnapshotProducts`
    - A query do run filtra `status = 'success'` (grep literal em catalog-store.js encontra `status = 'success'`)
    - `cd app-partners-recomendados && node -e "import('./src/db/catalog-store.js').then(m => { if (typeof m.getLatestSnapshotProducts !== 'function' || m.db !== undefined) { console.error('contrato violado: função ausente ou objeto db exposto'); process.exit(1); } const rows = m.getLatestSnapshotProducts(); if (!Array.isArray(rows) || rows.length === 0) { console.error('esperado array não-vazio com o catalog.db real já ingerido'); process.exit(1); } const k = ['productId','name','colorValue','fabricTagCanonical','hasAvailableGrade','variants']; const s = rows[0]; if (!k.every(key => key in s) || !Array.isArray(s.variants) || typeof s.hasAvailableGrade !== 'boolean') { console.error('shape inesperado: ' + JSON.stringify(Object.keys(s))); process.exit(1); } console.log('OK ' + rows.length + ' produtos no snapshot'); })"` sai com código 0 e imprime a contagem (com o `data/catalog.db` real, ~645 produtos — leitura somente, nenhuma escrita)
    - Funções e comportamento existentes de catalog-store.js preservados: `node -e` de contrato da Fase 2 continua válido (startIngestionRun, persistIngestionBatch, finishIngestionRun, getCanonicalMap seguem exportadas)
  </acceptance_criteria>
  <verify>
    <automated>cd app-partners-recomendados && node -e "import('./src/db/catalog-store.js').then(m => { const rows = m.getLatestSnapshotProducts(); const k = ['productId','name','colorValue','fabricTagCanonical','hasAvailableGrade','variants']; if (!Array.isArray(rows) || rows.length === 0 || !k.every(key => key in rows[0])) { console.error('FALHOU'); process.exit(1); } console.log('OK ' + rows.length); })"</automated>
  </verify>
  <done>getLatestSnapshotProducts() lê o último run success e devolve o snapshot completo no shape CatalogProductEntry (cor de variants.color_value per IN-03, boolean explícito para hasAvailableGrade, variantes do mesmo run), sem filtrar elegibilidade, sem expor db, via prepared statements com parâmetros nomeados.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: recommend-cli.js — preview determinístico do motor sobre o snapshot real</name>
  <files>app-partners-recomendados/src/recommendation/recommend-cli.js</files>
  <read_first>
    app-partners-recomendados/src/recommendation/recommendation-engine.js
    app-partners-recomendados/src/db/catalog-store.js
    .planning/phases/03-motor-de-recomenda-o-determin-stico/03-CONTEXT.md
  </read_first>
  <action>
    Criar `src/recommendation/recommend-cli.js`, um script Node ESM somente-leitura (sem exports) com exatamente duas importações de módulo local: `recommendForProduct` de `./recommendation-engine.js` e `getLatestSnapshotProducts` de `../db/catalog-store.js`. Nenhuma outra importação — em particular, nenhuma referência ao client HTTP da Nuvemshop, nenhum acesso de rede (o CLI opera 100% sobre `data/catalog.db` local, Success Criteria #4).

    Comportamento: lê `process.argv[2]` como productId; se ausente, imprime em stderr a linha de uso `Uso: node src/recommendation/recommend-cli.js <productId>` e sai com código 1; caso contrário chama `recommendForProduct(productId, getLatestSnapshotProducts())` e imprime em stdout SOMENTE `JSON.stringify(resultado, null, 2)`, saindo com código 0. Nenhum timestamp, contador ou dado variável no stdout — a saída precisa ser byte a byte idêntica entre execuções com o mesmo banco (RULE-02, determinismo verificável por diff).

    Header de comentário citando: D-17 (chamada produto a produto — este CLI é o primeiro consumidor real da interface do motor, prenúncio do preview da Fase 4) e o comportamento real esperado hoje: enquanto a planilha de tecidos não for importada (0/645 produtos com tecido canônico), o motor estrito (D-15/D-16) devolve lista vazia para qualquer produto real — saída `[]` é o resultado CORRETO nesta janela, não indício de bug; após a importação, o mesmo comando passa a devolver recomendações reais sem mudança de código. Não implementar nenhum modo alternativo de elegibilidade no CLI — ele apenas delega ao motor.
  </action>
  <acceptance_criteria>
    - `cd app-partners-recomendados && node src/recommendation/recommend-cli.js` (sem argumento) sai com código diferente de 0 e imprime a linha de uso em stderr
    - `cd app-partners-recomendados && node src/recommendation/recommend-cli.js 349886153` sai com código 0 e o stdout é JSON válido de um array (produto real da Fase 1; se não pertencer ao snapshot de Vestidos, o resultado é `[]` — ainda válido)
    - Duas execuções consecutivas com o mesmo productId produzem stdout idêntico (diff vazio — determinismo sobre o snapshot real)
    - `cd app-partners-recomendados && grep -v '^\s*//' src/recommendation/recommend-cli.js | grep -c '^import ' | grep -qx '2'` sai com código 0 (exatamente as duas importações locais previstas)
    - `cd app-partners-recomendados && ! grep -v '^\s*//' src/recommendation/recommend-cli.js | grep -Eq 'fetch\(|nuvemshop-client|node:https'` sai com código 0 (nenhum caminho de rede no CLI)
  </acceptance_criteria>
  <verify>
    <automated>cd app-partners-recomendados && node src/recommendation/recommend-cli.js 349886153 > /tmp/rec-run1.json && node src/recommendation/recommend-cli.js 349886153 > /tmp/rec-run2.json && diff /tmp/rec-run1.json /tmp/rec-run2.json && node -e "const r = JSON.parse(require('fs').readFileSync('/tmp/rec-run1.json','utf8')); if (!Array.isArray(r)) process.exit(1); console.log('OK: array determinístico com ' + r.length + ' recomendações')"</automated>
  </verify>
  <done>CLI somente-leitura roda o motor contra o snapshot real com saída JSON byte a byte determinística, delegando 100% da regra ao motor (zero lógica de elegibilidade própria), sem rede; comportamento de lista vazia na janela pré-planilha documentado no header.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dados persistidos (originados da API externa) → nova query de leitura | Nomes/tags/valores em SQLite vieram de API externa; a leitura não pode montar SQL com esses valores |
| Argumento argv do operador → CLI | productId vindo da linha de comando é input não confiável |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering (SQL injection no novo caminho de leitura) | `catalog-store.js` | mitigate | Prepared statements com parâmetro nomeado `@runId` derivado internamente do statement de runs; nenhum valor externo (argv, dado de produto) entra em string SQL |
| T-03-05 | Tampering/Spoofing (argv malicioso no CLI) | `recommend-cli.js` | mitigate | `process.argv[2]` é usado apenas como chave de comparação em memória dentro do motor — nunca em SQL, eval, ou construção de caminho de arquivo |
| T-03-06 | Information Disclosure (saída do CLI) | `recommend-cli.js` | accept | Imprime apenas dados de catálogo já públicos no storefront (produto, cor, tecido, estoque agregado); nenhum token/credencial passa por este caminho |
| T-03-SC | Tampering (supply chain em instalação de pacote) | npm | accept | Nenhum pacote novo é instalado neste plano — apenas better-sqlite3 já instalado/auditado na Fase 2 |
</threat_model>

<verification>
- `node -e` de shape: `getLatestSnapshotProducts()` retorna array não-vazio no shape CatalogProductEntry contra o `data/catalog.db` real (leitura somente)
- Contrato preservado: catalog-store.js continua sem expor o objeto db e com as funções da Fase 2 intactas
- CLI: dupla execução com diff vazio (determinismo sobre snapshot real) + JSON válido + exit 1 com uso quando sem argumento
- `cd app-partners-recomendados && npx vitest run` — suíte completa do projeto continua verde (nenhuma regressão nos testes das Fases 2 e do plano 03-01)
</verification>

<success_criteria>
- Fatia vertical completa: do `data/catalog.db` real até a saída de recomendações do motor, operável por um comando (`node src/recommendation/recommend-cli.js <productId>`)
- Success Criteria #3 comprovado também sobre o snapshot real (duas execuções, saída idêntica)
- Success Criteria #4 comprovado no caminho completo: nenhum acesso de rede em motor, leitura ou CLI
- Decisão de discretion IN-03 exercida e documentada: cor de `variants.color_value`
- Comportamento pré-planilha (`[]` para produtos reais sem tecido canônico) explicitamente documentado como correto (D-15/D-16), evitando falso alarme na semana da importação
</success_criteria>

<output>
Create `.planning/phases/03-motor-de-recomenda-o-determin-stico/03-02-SUMMARY.md` when done
</output>
