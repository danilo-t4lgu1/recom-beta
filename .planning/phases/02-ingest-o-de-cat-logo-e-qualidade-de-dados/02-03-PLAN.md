---
phase: 02-ingest-o-de-cat-logo-e-qualidade-de-dados
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - app-partners-recomendados/src/ingestion/ingest-catalog.js
  - app-partners-recomendados/src/db/catalog-store.js
  - app-partners-recomendados/scripts/run-ingestion.js
autonomous: false
requirements: [PLAT-02, DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - "O estado atual das recomendações geridas pelo sistema (Metafields namespace 'recomendados') é lido para cada produto da categoria Vestidos ANTES de qualquer nova computação, e persistido em recommendation_baseline como registro informativo (DATA-02) — sem lógica de comparação/drift (D-12)"
    - "Uma execução completa e real do job de ingestão contra a categoria Vestidos inteira (628 produtos esperados) termina sem 429 (rate limit respeitado) e sem exceção não tratada"
    - "Ao final da execução real, o número de produtos persistidos em SQLite é consistente com a contagem real retornada por GET /products?category_id=X (não travado em 628 se o número real divergir — D-02 exige confirmar o número real, não a estimativa)"
    - "O orçamento real de chamadas de Metafields por produto (Pitfall B) foi observado na execução real e documentado no SUMMARY (se um endpoint de listagem em lote de Metafields existir e for descoberto durante a execução, usá-lo; caso contrário aceitar o custo de 1 chamada por produto)"
  artifacts:
    - path: "app-partners-recomendados/src/ingestion/ingest-catalog.js"
      provides: "runIngestion() estendido para ler baseline de Metafields por produto (DATA-02) antes de persistir"
      contains: "getMetafields"
    - path: "app-partners-recomendados/src/db/catalog-store.js"
      provides: "persistIngestionBatch estendido para gravar recommendation_baseline junto com products/variants/snapshots na mesma transação"
      contains: "recommendation_baseline"
    - path: "app-partners-recomendados/scripts/run-ingestion.js"
      provides: "script executável de entrada única (node scripts/run-ingestion.js) que chama runIngestion() e imprime um resumo final (produtos lidos, disponíveis, tags não mapeadas, baseline lido)"
      min_lines: 10
  key_links:
    - from: "app-partners-recomendados/src/ingestion/ingest-catalog.js"
      to: "app-partners-recomendados/src/nuvemshop-client/client.js"
      via: "runIngestion() chama getMetafields({ ownerId: product.id }) para cada produto antes da persistência final, reaproveitando a função já existente da Fase 1"
      pattern: "getMetafields\\(\\{\\s*ownerId"
    - from: "app-partners-recomendados/scripts/run-ingestion.js"
      to: "app-partners-recomendados/src/ingestion/ingest-catalog.js"
      via: "import { runIngestion } from '../src/ingestion/ingest-catalog.js'"
      pattern: "runIngestion"
---

<objective>
Completar a fatia vertical de ingestão adicionando a leitura de baseline de recomendações atuais (DATA-02, Metafields por produto, reaproveitando `getMetafields()` já validado na Fase 1) ao orquestrador, e provar a fase inteira funcionando de ponta a ponta contra a categoria Vestidos real e completa (não uma amostra) — sem erros de rate limit, com estoque lido corretamente, tags auditadas, e histórico versionado gravado.

Purpose: esta é a wave que efetivamente satisfaz o Success Criteria da Fase 2 no ROADMAP.md — uma leitura completa dos produtos reais sem erros de rate limit, com baseline de recomendações lido antes de qualquer computação futura da Fase 3.
Output: execução real completa registrada (`ingestion_runs` com `status='success'`, contagem real de produtos), script de entrada única reutilizável para as próximas execuções (diárias, na Fase 6).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ingest-o-de-cat-logo-e-qualidade-de-dados/02-CONTEXT.md
@.planning/phases/02-ingest-o-de-cat-logo-e-qualidade-de-dados/02-RESEARCH.md
@.planning/phases/02-ingest-o-de-cat-logo-e-qualidade-de-dados/02-02-SUMMARY.md
</context>

## Artifacts this phase produces (deste plano)

| Artifact | Type | Provides |
|----------|------|----------|
| `runIngestion()` (estendido, `ingest-catalog.js`) | função modificada | Adiciona leitura de baseline de Metafields por produto (DATA-02) antes da persistência final |
| `persistIngestionBatch` (estendido, `catalog-store.js`) | função modificada | Grava `recommendation_baseline` na mesma transação de products/variants/snapshots |
| `getMetafields` (estendido, `client.js`, se necessário) | função modificada | Aceita `limiter` opcional para respeitar rate limit em volume (~628 chamadas) |
| `scripts/run-ingestion.js` | script CLI novo | Ponto de entrada único e reutilizável do job de ingestão (reuso futuro na Fase 6) |
| `data/catalog.db` (dado real gerado) | artefato de execução | Primeira execução real e completa da ingestão da categoria Vestidos persistida |

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ler baseline de recomendações (Metafields) por produto e persistir (DATA-02)</name>
  <files>app-partners-recomendados/src/ingestion/ingest-catalog.js, app-partners-recomendados/src/db/catalog-store.js</files>
  <read_first>
    app-partners-recomendados/src/ingestion/ingest-catalog.js
    app-partners-recomendados/src/db/catalog-store.js
    app-partners-recomendados/src/db/schema.sql
    app-partners-recomendados/src/api/recommendations.js
    app-partners-recomendados/src/nuvemshop-client/client.js
    .planning/phases/02-ingest-o-de-cat-logo-e-qualidade-de-dados/02-RESEARCH.md
  </read_first>
  <action>
    Em `src/ingestion/ingest-catalog.js`, dentro de `runIngestion()`, adicionar a etapa de leitura de baseline: para cada produto já paginado (após a resolução de categoria, antes da persistência final), chamar `getMetafields({ ownerId: product.id })` (import já existente de `../nuvemshop-client/client.js`, reaproveitando exatamente a mesma função usada em `src/api/recommendations.js` linha 28) através de `fetchWithRateLimit`/limiter (a chamada HTTP dentro de `getMetafields` já usa `fetch` direto hoje — se `getMetafields` não aceitar um `limiter` opcional, adicionar esse parâmetro opcional a `getMetafields` em `client.js` da mesma forma que foi feito para `listCategories`/`listProducts` no Plano 01, para que a leitura de baseline de ~628 chamadas também respeite o rate limit adaptativo, per Pitfall B do RESEARCH.md). Filtrar o resultado por `namespace === 'recomendados' && key === 'produto_sugerido'` (mesmo padrão de `getRecommendations` em `recommendations.js`), extrair `currentValue` (ou `null` se ausente — produto sem recomendação prévia não é erro).

    Montar um registro `{ productId: product.id, runId, currentRecommendedProductId: currentValue, readAt: new Date().toISOString() }` por produto e incluir esses registros no batch passado para `persistIngestionBatch`.

    Em `src/db/catalog-store.js`, estender `persistIngestionBatch` para incluir `insertRecommendationBaseline = db.prepare('INSERT INTO recommendation_baseline (product_id, run_id, current_recommended_product_id, read_at) VALUES (@productId, @runId, @currentRecommendedProductId, @readAt)')` dentro da mesma transação `db.transaction()` já existente (não uma transação separada) — DATA-02 é apenas um registro informativo (D-12), sem lógica de comparação/drift.
  </action>
  <acceptance_criteria>
    - `ingest-catalog.js` contém uma chamada a `getMetafields({ ownerId:` dentro de `runIngestion`
    - `catalog-store.js` contém `INSERT INTO recommendation_baseline` dentro da mesma função `db.transaction()` usada para produtos/variantes/snapshots (não uma segunda transação separada)
    - A leitura de baseline não lança exceção para produtos sem Metafield de recomendação prévio (retorna `null`, não erro)
  </acceptance_criteria>
  <verify>
    <automated>cd app-partners-recomendados && node -e "import('./src/ingestion/ingest-catalog.js').then(m => { if (typeof m.runIngestion !== 'function') { console.error('runIngestion nao exportado'); process.exit(1); } console.log('OK'); })"</automated>
  </verify>
  <done>runIngestion() lê o Metafield de recomendação de cada produto antes de persistir, respeitando o rate limiter; recommendation_baseline gravado na mesma transação que products/variants/snapshots.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Script de execução única e execução real completa contra Vestidos</name>
  <files>app-partners-recomendados/scripts/run-ingestion.js</files>
  <read_first>
    app-partners-recomendados/scripts/roundtrip-metafield.js
    app-partners-recomendados/scripts/resolve-category.js
    app-partners-recomendados/src/ingestion/ingest-catalog.js
  </read_first>
  <action>
    Criar `scripts/run-ingestion.js` seguindo o mesmo estilo standalone de `scripts/roundtrip-metafield.js`/`scripts/resolve-category.js`: importa `runIngestion` de `'../src/ingestion/ingest-catalog.js'`, aceita o nome da categoria como `process.argv[2]` (default `'Vestidos'`), chama `await runIngestion({ categoryName })`, e imprime um resumo final via `console.log` contendo: total de produtos lidos, quantos foram calculados como "disponível" (`hasAvailableGrade === true`), quantas tags brutas distintas foram vistas na auditoria (`frequency.size`), quantas ficaram não mapeadas (`unmapped.size` — esperado ser igual ao total de tags vistas, per D-06, já que nenhuma tag está preenchida ainda), e quantos produtos tinham um `recommendation_baseline` não-nulo (recomendação prévia já existente). Sai com `process.exit(0)` em sucesso e `process.exit(1)` logando `err.message` em falha, mesmo padrão de tratamento de erro de `roundtrip-metafield.js`.
  </action>
  <acceptance_criteria>
    - `scripts/run-ingestion.js` importa `runIngestion` de `'../src/ingestion/ingest-catalog.js'`
    - Script imprime pelo menos: contagem total de produtos, contagem de produtos disponíveis, contagem de tags não mapeadas
  </acceptance_criteria>
  <verify>
    <automated>cd app-partners-recomendados && node -e "import('fs').then(fs => { if (!fs.existsSync('./scripts/run-ingestion.js')) { console.error('scripts/run-ingestion.js nao existe'); process.exit(1); } console.log('OK'); })"</automated>
  </verify>
  <done>Script de entrada única criado, pronto para reuso na Fase 6 (operação diária).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Executar o job de ingestão completo contra a categoria Vestidos real e confirmar sucesso</name>
  <files>app-partners-recomendados/data/catalog.db</files>
  <read_first>
    app-partners-recomendados/scripts/run-ingestion.js
    app-partners-recomendados/src/ingestion/ingest-catalog.js
  </read_first>
  <what-built>Job de ingestão completo (Planos 01+02+03): lê a categoria Vestidos real via API pública paginada, calcula disponibilidade de estoque via inventory_levels[], audita tags de tecido, lê baseline de recomendações via Metafields, e persiste tudo em SQLite numa transação por execução — respeitando rate limit adaptativo.</what-built>
  <action>Executar `scripts/run-ingestion.js` contra a loja real Talgui e confirmar, via inspeção do resumo impresso e do arquivo SQLite gerado, que a execução foi bem-sucedida e os números fazem sentido.</action>
  <how-to-verify>
    1. Dentro de `app-partners-recomendados/`, rodar: `node --env-file=.env scripts/run-ingestion.js Vestidos` (ajustar a forma de carregar `.env` conforme confirmado no Plano 01/roundtrip-metafield.js).
    2. Confirmar que o script termina com código de saída 0, sem exceção não tratada, e sem nenhuma linha de log indicando `429`.
    3. Confirmar no resumo impresso que a contagem total de produtos lidos é próxima de 628 (D-01) — se divergir significativamente, isso pode indicar um `category_id` resolvido errado (Pitfall C) ou uma categoria com subcategorias/produtos arquivados; reportar o número real observado, que deve ser usado para atualizar D-02/PROJECT.md depois (fora do escopo de código desta fase).
    4. Inspecionar `data/catalog.db` (ex: `sqlite3 data/catalog.db "SELECT status, products_read FROM ingestion_runs ORDER BY id DESC LIMIT 1;"` ou equivalente) e confirmar `status = 'success'`.
    5. Confirmar que `data/catalog.db` NÃO aparece em `git status` (protegido pelo `.gitignore`).
  </how-to-verify>
  <acceptance_criteria>
    - Execução termina com exit code 0, sem linha de log indicando status 429
    - `ingestion_runs.status = 'success'` na última linha da tabela
    - Contagem de produtos lidos é próxima de 628 (D-01) ou o número real observado está documentado como divergente com explicação
    - `data/catalog.db` não aparece em `git status`
  </acceptance_criteria>
  <verify>
    <automated>MISSING — execução real contra a loja Talgui e inspeção de dados reais; não substituível por comando determinístico sem acesso à loja real</automated>
  </verify>
  <done>Execução real completa confirmada pelo usuário: ingestão bem-sucedida, contagem de produtos plausível, data/catalog.db protegido do git.</done>
  <resume-signal>Digite "aprovado" se a execução real terminou com sucesso e os números fazem sentido, ou descreva o problema encontrado (ex: contagem muito diferente de 628, erro 429, exceção) para correção antes de fechar a fase.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Job de ingestão → ~628 chamadas de Metafields | Volume que se aproxima do burst do rate limit (40) — throttling real precisa segurar essa etapa, não só a paginação de produtos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Denial of Service (rate-limit exhaustion na leitura de baseline em volume) | `ingest-catalog.js` (loop de `getMetafields` por produto) | mitigate | `getMetafields` passa a aceitar o mesmo `limiter`/`fetchWithRateLimit` usado em `listProducts`/`listCategories` (Plano 01), garantindo throttling real também nas ~628 chamadas de baseline, não só na paginação (Pitfall B) |
| T-02-09 | Repudiation (execução real sem registro de auditoria se falhar no meio) | `ingest-catalog.js` / `catalog-store.js` | mitigate | `finishIngestionRun` com `status: 'failed'` sempre chamado em caso de exceção (já implementado no Plano 02), permitindo rastrear execuções que não completaram |
</threat_model>

<verification>
- `node --env-file=.env scripts/run-ingestion.js Vestidos` roda até o fim sem exceção, sem 429
- `ingestion_runs.status = 'success'` na última linha da tabela após a execução
- Contagem de produtos persistidos consistente com a contagem real retornada pela API (não hardcoded/assumida)
- `recommendation_baseline` populado com uma linha por produto por execução
- `git status` não lista `data/catalog.db`
</verification>

<success_criteria>
- Leitura completa da categoria Vestidos real executada sem erro de rate limit, com throttling adaptativo comprovado em volume real (não só na paginação de produtos)
- Baseline de recomendações (DATA-02) lido e persistido para cada produto antes de qualquer computação futura da Fase 3
- Execução real registrada em `ingestion_runs` com contagem real de produtos (base para atualizar D-02/PROJECT.md com o número real do catálogo, fora do escopo de código desta fase)
- `data/*.db` nunca commitado
</success_criteria>

<output>
Create `.planning/phases/02-ingest-o-de-cat-logo-e-qualidade-de-dados/02-03-SUMMARY.md` when done
</output>
