---
phase: 07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app-partners-recomendados/src/review/write-executor.js
  - app-partners-recomendados/src/review/write-executor.test.js
autonomous: true
requirements: [APRV-03, WRTE-02, WRTE-04]
must_haves:
  truths:
    - "Existe um caminho de escrita `scheduled` que grava SEM passar pelo gate de aprovação (aposenta o portão prévio do APRV-03) (D-61)"
    - "O caminho scheduled grava triggered_by:'scheduled' em write_log, nunca 'manual' nem fabricando decisão approved (D-61)"
    - "executeApprovedWrite continua exigindo aprovação como primeira operação — o gate NÃO é reaberto (APRV-03 preservado para o caminho manual)"
    - "Defesa 2 (D-67): cada id recomendado é reconferido contra o snapshot atual (existe, visível, com estoque, mesma cor); ids que falham são descartados"
    - "Conjunto vazio após a Defesa 2 vira lacuna de cobertura registrada (written:false), nunca lixo gravado (D-67)"
    - "dryRun no caminho scheduled retorna cedo com zero I/O, reusando o padrão do APRV-04 (base do kill switch D-62)"
  artifacts:
    - path: "app-partners-recomendados/src/review/write-executor.js"
      provides: "executeScheduledWrite (sem gate, triggered_by scheduled) + filterReferentiallyValid (Defesa 2)"
      contains: "executeScheduledWrite"
    - path: "app-partners-recomendados/src/review/write-executor.test.js"
      provides: "cobertura do caminho scheduled, Defesa 2 e preservação do gate manual"
  key_links:
    - from: "orquestrador diário (Plano 07-05)"
      to: "app-partners-recomendados/src/review/write-executor.js (executeScheduledWrite)"
      via: "chamada por produto-fonte elegível com snapshotById + sourceEntry + dryRun + runId"
      pattern: "executeScheduledWrite"
---

<objective>
Adicionar ao **ponto único de escrita** (`write-executor.js`) um caminho `scheduled` que grava automaticamente **sem o gate de aprovação prévia** (D-61, reverte APRV-03 para o modo automático) e um **portão final referencial (Defesa 2, D-67)** que descarta ids inválidos antes de gravar — sem fabricar uma decisão `approved` (o que o protótipo `_batch-write.js` fazia) e sem enfraquecer o gate do caminho manual existente.

Purpose: o job diário (Plano 07-05) precisa de um caminho de escrita real, auditável e defendido contra corrida/oculto/404, reaproveitando toda a mecânica de `findMetafield`/`updateMetafield`/`createMetafield`/`insertWriteLog`/`notifyWriteFailure` já validada em produção na Fase 5.
Output: `executeScheduledWrite(...)`, `filterReferentiallyValid(...)`, e um helper de escrita compartilhado com `triggered_by` parametrizado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-CONTEXT.md
@.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-RESEARCH.md
@.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Defesa 2 referencial (D-67) — filterReferentiallyValid</name>
  <files>app-partners-recomendados/src/review/write-executor.js, app-partners-recomendados/src/review/write-executor.test.js</files>
  <read_first>
    - app-partners-recomendados/src/review/write-executor.js (estrutura de `executeApprovedWrite` l.36-87; `JSON.stringify(approvedIds)` l.43)
    - app-partners-recomendados/src/db/catalog-store.js (shape de `getLatestSnapshotProducts`, campos productId/colorValue/hasAvailableGrade/published — este é o shape do Map de snapshot)
    - app-partners-recomendados/src/recommendation/recommendation-engine.js (`normalizeMatchValue` — mesma convenção de normalização de cor)
    - .planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-RESEARCH.md §"Code Examples > Defesa 2" (D-67)
  </read_first>
  <action>
    Escreva testes primeiro (RED). Exporte `filterReferentiallyValid(sourceEntry, recommendedIds, snapshotById)`: dado o `sourceEntry` (CatalogProductEntry da fonte) e um `Map<string, CatalogProductEntry>` do snapshot atual (`snapshotById`), devolva apenas os ids cujo candidato: existe no `snapshotById`, tem `published !== false` (oculto descartado, D-58/D-67), tem `hasAvailableGrade` verdadeiro (estoque), e cuja cor normalizada bate com a cor normalizada da fonte. Use a mesma normalização de cor (trim + minúsculas) empregada no motor. Função pura, sem I/O, sem lançar para entradas ausentes.
  </action>
  <acceptance_criteria>
    - Teste: um id ausente do `snapshotById` é descartado.
    - Teste: um id com `published: false` é descartado; com `published: null` NÃO é descartado por visibilidade.
    - Teste: um id sem `hasAvailableGrade` é descartado; um id com cor diferente da fonte é descartado.
    - Teste: entrada com todos válidos preserva a ordem/conteúdo dos ids válidos.
    - `cd app-partners-recomendados && npx vitest run src/review/write-executor.test.js` sai com código 0.
  </acceptance_criteria>
  <verify>
    <automated>cd app-partners-recomendados && npx vitest run src/review/write-executor.test.js</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Caminho scheduled sem gate (D-61) + write_log triggered_by scheduled</name>
  <files>app-partners-recomendados/src/review/write-executor.js, app-partners-recomendados/src/review/write-executor.test.js</files>
  <read_first>
    - app-partners-recomendados/src/review/write-executor.js (todo o arquivo: `assertApproved` como 1ª operação l.37; ramo `dryRun` early-return l.39-41; try/catch de escrita real l.45-86; `triggeredBy: 'manual'` l.59/72)
    - app-partners-recomendados/src/db/catalog-store.js (`insertWriteLog` — o tipo já aceita `triggeredBy: 'manual'|'scheduled'|'rollback'`, l.441)
    - app-partners-recomendados/src/review/notify-failure.js (`notifyWriteFailure` — reuso no ramo de falha)
    - .planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-RESEARCH.md §"Pitfall 3" e §"Anti-Patterns" (não fabricar decisão approved; caminho scheduled explícito)
  </read_first>
  <action>
    Escreva testes primeiro (RED). Extraia a mecânica de escrita comum (findMetafield → updateMetafield/createMetafield → insertWriteLog de sucesso; no catch, insertWriteLog failed + `notifyWriteFailure(...).catch(() => {})` + relançar o erro original) para um helper interno `writeRecommendationMetafield({ productId, approvedIds, triggeredBy, runId })`, parametrizando `triggeredBy`. Faça `executeApprovedWrite` reusar esse helper com `triggeredBy: 'manual'`, mantendo `assertApproved` como PRIMEIRA operação e o early-return de `dryRun` INALTERADOS (APRV-03 preservado para o caminho manual). Adicione `executeScheduledWrite({ productId, recommendedIds, dryRun, runId, sourceEntry, snapshotById })`: NÃO chama `assertApproved` (D-61); primeiro aplica a Defesa 2 via `filterReferentiallyValid(sourceEntry, recommendedIds, snapshotById)`; se o conjunto ficar vazio, retorna `{ productId, approvedIds: [], written: false, reason: 'coverage-gap' }` (lacuna registrada, nenhuma escrita, D-67); respeita `dryRun` com early-return de zero I/O (reusa o padrão APRV-04, base do kill switch D-62); no modo real, grava via o helper compartilhado com `triggeredBy: 'scheduled'`. NUNCA construa um objeto `{ status: 'approved' }` (Anti-Pattern — o que `_batch-write.js` faz).
  </action>
  <acceptance_criteria>
    - Teste: `executeScheduledWrite` com `dryRun:false` e ids válidos grava e registra uma linha em write_log com `triggeredBy: 'scheduled'` (nunca 'manual').
    - Teste: `executeScheduledWrite` NÃO chama `assertApproved` — o caminho grava sem qualquer objeto de decisão.
    - Teste: `executeScheduledWrite` com todos os ids inválidos pela Defesa 2 retorna `written:false`, `reason:'coverage-gap'` e NÃO chama update/create/insertWriteLog de sucesso.
    - Teste: `executeScheduledWrite` com `dryRun:true` não faz nenhuma chamada de rede/DB (stubs de client.js nunca invocados).
    - Teste de regressão: `executeApprovedWrite` continua lançando `ApprovalRequiredError` quando a decisão não é approved (gate intacto).
    - `cd app-partners-recomendados && npx vitest run src/review/write-executor.test.js` sai com código 0.
  </acceptance_criteria>
  <verify>
    <automated>cd app-partners-recomendados && npx vitest run src/review/write-executor.test.js</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orquestrador → write-executor | O orquestrador (não confiável quanto a corridas/estado obsoleto) fornece ids recomendados + snapshot; a Defesa 2 revalida antes de gravar. |
| write-executor → API pública Nuvemshop | Escrita real de Metafield com token OAuth (V2) — nunca logado no payload de notificação (V7). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Integrity / Information Disclosure | Gravar id de produto oculto/sem estoque/cor errada (404 no storefront) | mitigate | Defesa 2 `filterReferentiallyValid` descarta ids inválidos antes do `JSON.stringify`; conjunto vazio vira lacuna, nunca lixo (D-67). |
| T-07-08 | Elevation of Privilege | Caminho automático burlar auditoria/gate fabricando decisão approved | mitigate | Caminho `scheduled` explícito, `triggered_by:'scheduled'` em write_log; `executeApprovedWrite` mantém `assertApproved` como 1ª operação (APRV-03 preservado no manual). |
| T-07-09 | Repudiation | Escrita automática sem rastro de auditoria | mitigate | Toda tentativa (sucesso/falha) grava 1 linha em `write_log` (WRTE-02/WRTE-04); falha dispara `notifyWriteFailure` sem mascarar o erro. |
| T-07-SC | Tampering | Instalação de pacote (supply chain) | accept | Nenhuma dependência nova (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

## Artefatos que esta fase produz

- `write-executor.js`: `executeScheduledWrite({ productId, recommendedIds, dryRun, runId, sourceEntry, snapshotById })` (exportada); `filterReferentiallyValid(sourceEntry, recommendedIds, snapshotById)` (exportada); helper interno `writeRecommendationMetafield({ productId, approvedIds, triggeredBy, runId })`.
- Nova convenção de `write_log`: `triggered_by = 'scheduled'` para escritas do job automático.

<verification>
- `cd app-partners-recomendados && npx vitest run src/review/write-executor.test.js` verde.
- Regressão: gate do caminho manual intacto (`ApprovalRequiredError`).
</verification>

<success_criteria>
- Caminho `scheduled` grava sem gate, com `triggered_by:'scheduled'`, dry-run com zero I/O (D-61/D-62).
- Defesa 2 descarta ids inválidos; conjunto vazio vira lacuna registrada (D-67).
- Caminho manual (APRV-03) preservado; nenhuma regressão.
</success_criteria>

<output>
Create `.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-03-SUMMARY.md` when done
</output>
