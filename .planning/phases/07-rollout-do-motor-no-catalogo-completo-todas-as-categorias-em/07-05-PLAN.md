---
phase: 07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em
plan: 05
type: execute
wave: 2
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - app-partners-recomendados/scripts/run-daily-job.js
  - app-partners-recomendados/scripts/run-daily-job.test.js
  - app-partners-recomendados/src/review/circuit-breaker.js
  - app-partners-recomendados/src/review/circuit-breaker.test.js
  - app-partners-recomendados/src/review/notify-failure.js
  - app-partners-recomendados/src/review/notify-failure.test.js
  - .github/workflows/daily-recompute.yml
autonomous: true
requirements: [RULE-03, FEED-01, APRV-03, WRTE-04, WRTE-05]
user_setup:
  - service: github-actions
    why: "Kill switch operacional do regime diário (D-62) — sem depender da máquina do usuário"
    dashboard_config:
      - task: "Criar a repository variable WRITE_ENABLED (ausente/false => dry-run; 'true' => grava)"
        location: "GitHub > Settings > Secrets and variables > Actions > Variables"
must_haves:
  truths:
    - "O job diário passa a INGERIR, CALCULAR e GRAVAR automaticamente (muda D-47), guardado por kill switch + disjuntor + Defesa 1 + Defesa 2 (D-61/D-68)"
    - "Kill switch (D-62): com WRITE_ENABLED off (ou WRITE_OVERRIDE=false), o job calcula e loga o que gravaria mas passa dryRun=true a cada escrita — nenhuma escrita real"
    - "Defesa 1 (D-66): se alguma das 11 categorias voltar com 0 produtos, ou o total sair da banda vs. o último run, o job ABORTA a escrita e notifica — leitura truncada nunca vira apagão"
    - "Disjuntor (D-63): churn > 30% ou apagão > 10% aborta a escrita e notifica; o 1º rollout (FIRST_ROLLOUT) é isento"
    - "Só produtos-fonte com estoque disponível e visíveis recebem escrita (D-54); só entram os com diff real vs. baseline (D-68)"
    - "Um resumo diário (alterados/zerados/novos) é enviado pelo webhook, sem exigir conferência manual do painel (D-69)"
    - "As 11 categorias vêm de ALL_TAXONOMY_CATEGORY_NAMES (fonte única), nunca digitadas no YAML (D-53)"
  artifacts:
    - path: "app-partners-recomendados/scripts/run-daily-job.js"
      provides: "orquestração: resolveWriteEnabled (D-62), Defesa 1 (D-66), disjuntor (D-63), escrita scheduled (D-61), resumo diário (D-69)"
      contains: "resolveWriteEnabled"
    - path: "app-partners-recomendados/src/review/circuit-breaker.js"
      provides: "tripBreaker puro (churn/apagão) + setsEqual"
      contains: "tripBreaker"
    - path: "app-partners-recomendados/src/review/notify-failure.js"
      provides: "notifyDailySummary reusando o webhook (D-69)"
      contains: "notifyDailySummary"
    - path: ".github/workflows/daily-recompute.yml"
      provides: "inputs write/first_rollout + env WRITE_ENABLED/WRITE_OVERRIDE/FIRST_ROLLOUT (D-62)"
      contains: "WRITE_ENABLED"
  key_links:
    - from: "app-partners-recomendados/scripts/run-daily-job.js"
      to: "app-partners-recomendados/src/review/write-executor.js (executeScheduledWrite)"
      via: "escrita por produto-fonte elegível com snapshotById + sourceEntry + dryRun + runId"
      pattern: "executeScheduledWrite"
    - from: "app-partners-recomendados/scripts/run-daily-job.js"
      to: "app-partners-recomendados/src/db/catalog-store.js (getLastWrittenValuesForAllProducts)"
      via: "baseline de conjunto do disjuntor"
      pattern: "getLastWrittenValuesForAllProducts"
    - from: ".github/workflows/daily-recompute.yml"
      to: "app-partners-recomendados/scripts/run-daily-job.js (resolveWriteEnabled)"
      via: "vars.WRITE_ENABLED / github.event.inputs.write mapeados para env"
      pattern: "vars.WRITE_ENABLED"
---

<objective>
Transformar o orquestrador diário `run-daily-job.js` de "só enfileira aprovação" (D-47, Fase 6) em "**ingere → calcula → grava automaticamente**" (D-61/D-68), cercado por quatro guardas testáveis e explícitas: **kill switch** (D-62), **Defesa 1 de integridade do snapshot** (D-66), **disjuntor churn/apagão** (D-63) e a **Defesa 2 referencial** já embutida no `executeScheduledWrite` (D-67, Plano 07-03) — mais um **resumo diário** pelo webhook (D-69) e o toggle exposto no workflow do GitHub Actions (D-62).

Purpose: entregar o **fluxo recorrente de re-gravação automática** que mantém a vitrine sempre populada, com desligamento manual e automático, reaproveitando toda a infra existente (write-executor scheduled, write_log baseline, diff/queue, notify-failure, vars.* no workflow).
Output: `resolveWriteEnabled`, sequência de guardas em `runDailyJob`, `circuit-breaker.js` (`tripBreaker`), `notifyDailySummary`, e o YAML com inputs/env do kill switch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-CONTEXT.md
@.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-RESEARCH.md
@.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-PATTERNS.md
@.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Kill switch (D-62) — resolveWriteEnabled + toggle no workflow</name>
  <files>app-partners-recomendados/scripts/run-daily-job.js, app-partners-recomendados/scripts/run-daily-job.test.js, .github/workflows/daily-recompute.yml</files>
  <read_first>
    - app-partners-recomendados/scripts/run-daily-job.js (todo o arquivo: `runDailyJob` l.58-79; bloco CLI ESM l.84-109; imports l.25-37)
    - app-partners-recomendados/src/ingestion/ingest-catalog.js (`resolveMinSizesInStock` l.30-33 — analog EXATO do padrão de toggle por env)
    - .github/workflows/daily-recompute.yml (todo o arquivo — env MIN_SIZES_IN_STOCK l.35-41 como analog; step de run l.42-45; commit-back l.47-57)
    - .planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-RESEARCH.md §"Pattern 3" e §"Code Examples > Kill switch" (D-62)
  </read_first>
  <action>
    Escreva o teste primeiro (RED). Em `run-daily-job.js`, adicione `resolveWriteEnabled()` espelhando `resolveMinSizesInStock`: `process.env.WRITE_OVERRIDE` (input do workflow_dispatch) tem prioridade — `'true'` → true, `'false'` → false; ausente cai em `process.env.WRITE_ENABLED === 'true'`; qualquer outro caso → false (ausente = dry-run seguro, A1/D-62). Derive `const dryRun = !resolveWriteEnabled()` e prepare-o para ser passado a cada escrita (será usado na Task 3). No `daily-recompute.yml`, adicione ao `on:` um `workflow_dispatch` com inputs `write` (boolean, default false — para o 1º rollout supervisionado D-64) e `first_rollout` (boolean, default false — isenção do disjuntor D-63); no step de run, adicione ao `env`: `WRITE_ENABLED: ${{ vars.WRITE_ENABLED }}`, `WRITE_OVERRIDE: ${{ github.event.inputs.write }}` e `FIRST_ROLLOUT: ${{ github.event.inputs.first_rollout }}`, mantendo `MIN_SIZES_IN_STOCK` e o step de commit-back inalterados.
  </action>
  <acceptance_criteria>
    - Teste unitário de `resolveWriteEnabled`: `WRITE_OVERRIDE='true'` → true; `WRITE_OVERRIDE='false'` → false (mesmo com `WRITE_ENABLED='true'`); ambos ausentes → false; `WRITE_ENABLED='true'` e `WRITE_OVERRIDE` ausente → true.
    - `grep -c 'WRITE_ENABLED' .github/workflows/daily-recompute.yml` retorna valor >= 1 e o step de run mantém `node scripts/run-daily-job.js`.
    - `grep -c 'workflow_dispatch' .github/workflows/daily-recompute.yml` retorna valor >= 1 com os inputs `write` e `first_rollout` presentes.
    - `cd app-partners-recomendados && npx vitest run scripts/run-daily-job.test.js` sai com código 0.
  </acceptance_criteria>
  <verify>
    <automated>cd app-partners-recomendados && npx vitest run scripts/run-daily-job.test.js</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Defesa 1 de integridade (D-66) + disjuntor churn/apagão (D-63)</name>
  <files>app-partners-recomendados/scripts/run-daily-job.js, app-partners-recomendados/scripts/run-daily-job.test.js, app-partners-recomendados/src/review/circuit-breaker.js, app-partners-recomendados/src/review/circuit-breaker.test.js</files>
  <read_first>
    - app-partners-recomendados/scripts/run-daily-job.js (sequência atual em `runDailyJob`: `runIngestion` l.69, `getLatestSnapshotProducts` l.71, `getLatestSuccessfulRunId` l.72; `notifyWriteFailure` já importado l.36)
    - app-partners-recomendados/src/db/catalog-store.js (`getLastWrittenValuesForAllProducts`, `getLastSuccessfulIngestionRunSummary` — providos pelo Plano 07-02)
    - app-partners-recomendados/src/ingestion/product-group.js (`ALL_TAXONOMY_CATEGORY_NAMES` l.77-89 — as 11 categorias, base da checagem "nenhuma com 0")
    - .planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-RESEARCH.md §"Pattern 4", §"Code Examples > Disjuntor", §"Pitfall 2/4", §"Open Questions" (baseline; banda; isenção do 1º rollout)
  </read_first>
  <action>
    Escreva os testes primeiro (RED). Crie o módulo puro `src/review/circuit-breaker.js` exportando `setsEqual(a, b)` (compara dois arrays de ids ignorando ordem, por String) e `tripBreaker({ toWrite, baseline, isFirstRollout, churnMax = 0.30, blackoutMax = 0.10 })` conforme o design do RESEARCH: `isFirstRollout` → não dispara (isento, D-63); senão calcula churn (fração de produtos cujo conjunto novo difere do baseline) e apagão (fração dos que TINHAM recomendação e ficaram vazios); retorna `{ trip: boolean, reason?: string }` disparando se `churn > churnMax` ou `blackout > blackoutMax`. Módulo zero-import, zero-I/O. Em `runDailyJob`, após `runIngestion`, adicione a **Defesa 1** (D-66): usando `ingestionResult.categoryCounts`, verifique que nenhuma das categorias solicitadas voltou com 0 produtos, e que o total lido está dentro de uma banda razoável vs. `getLastSuccessfulIngestionRunSummary().productsRead` (default proposto: total atual não pode cair abaixo de ~70% do último run — limiar à discrição, documentar); se falhar, chame `notifyWriteFailure({ productId: 'daily-job', error, triggeredBy: 'scheduled' })` e ABORTE a fase de escrita (retorne cedo com um resultado marcando `aborted: 'integrity'`), sem tocar a loja. Leia `isFirstRollout` de `process.env.FIRST_ROLLOUT === 'true'`. Deixe o disjuntor pronto para ser aplicado sobre o conjunto `toWrite` (montado na Task 3) antes de qualquer escrita real.
  </action>
  <acceptance_criteria>
    - Teste de `tripBreaker`: `isFirstRollout:true` nunca dispara, mesmo com churn/apagão de 100%.
    - Teste de `tripBreaker`: churn 40% (>30%) dispara com `reason` citando churn; apagão 20% (>10%) dispara com `reason` citando apagão; churn 10% e apagão 5% NÃO disparam.
    - Teste de Defesa 1 em `runDailyJob` (com mocks): quando `categoryCounts` tem uma categoria com 0 produtos, o job chama `notifyWriteFailure` e NÃO chama `executeScheduledWrite` em modo real; quando o total cai abaixo da banda, idem.
    - `grep -cE '^import ' app-partners-recomendados/src/review/circuit-breaker.js` retorna 0 (utilitário puro, zero-import).
    - `cd app-partners-recomendados && npx vitest run scripts/run-daily-job.test.js src/review/circuit-breaker.test.js` sai com código 0.
  </acceptance_criteria>
  <verify>
    <automated>cd app-partners-recomendados && npx vitest run scripts/run-daily-job.test.js src/review/circuit-breaker.test.js</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Escrita automática (D-61/D-68) + Defesa 2 wiring (D-67) + resumo diário (D-69)</name>
  <files>app-partners-recomendados/scripts/run-daily-job.js, app-partners-recomendados/scripts/run-daily-job.test.js, app-partners-recomendados/src/review/notify-failure.js, app-partners-recomendados/src/review/notify-failure.test.js</files>
  <read_first>
    - app-partners-recomendados/scripts/run-daily-job.js (sequência de `runDailyJob`; `buildReviewQueue` l.74; `seedPendingApprovalQueue` l.76)
    - app-partners-recomendados/src/review/write-executor.js (assinatura de `executeScheduledWrite` provida pelo Plano 07-03; Defesa 2 embutida)
    - app-partners-recomendados/src/review/review-queue.js (`hasChanged` l.28, `buildReviewQueue` l.53 — diff real, D-68)
    - app-partners-recomendados/src/recommendation/recommendation-engine.js (`recommendForProduct` — calcular recs por fonte)
    - app-partners-recomendados/src/review/notify-failure.js (`notifyWriteFailure` l.19-59 — padrão a espelhar em notifyDailySummary; nunca lança, degrada sem webhook, payload sem credencial)
    - .planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-RESEARCH.md §"Architecture Patterns" (diagrama de sequência), §"Open Questions 3/4" (fonte sem estoque; run_id da escrita)
  </read_first>
  <action>
    Escreva os testes primeiro (RED). Em `notify-failure.js`, adicione `notifyDailySummary({ summary })` seguindo o mesmo contrato de `notifyWriteFailure` (usa `WRITE_FAILURE_WEBHOOK_URL`, degrada silenciosamente se ausente, nunca lança, payload sem credencial), enviando um resumo textual `{ alterados, zerados, novos, aborted? }` (D-69). Em `runDailyJob`, após a Defesa 1 e o cálculo, monte o conjunto `toWrite`: para cada produto-fonte do snapshot que é elegível (`hasAvailableGrade` verdadeiro E `published !== false`, D-54), calcule `recommendForProduct(id, snapshot)` e compare com o baseline `getLastWrittenValuesForAllProducts()` via `hasChanged`; inclua apenas os com diff real (D-68). Trate a fonte que TINHA recomendação mas ficou inelegível (sem estoque/oculta) como um item de `toWrite` com conjunto vazio (apagão intencional, contabilizado pelo disjuntor — Open Question 3). Aplique `tripBreaker` sobre `toWrite` (isFirstRollout de `FIRST_ROLLOUT`); se disparar, `notifyWriteFailure` + aborta escrita (nenhuma gravação) e envia resumo com `aborted`. Caso contrário, para cada item de `toWrite` chame `executeScheduledWrite({ productId, recommendedIds, dryRun, runId: getLatestSuccessfulRunId(), sourceEntry, snapshotById })` (dryRun vindo do kill switch da Task 1; snapshotById = Map do snapshot atual para a Defesa 2, D-67; runId amarrado ao run do dia, Open Question 4). Acumule o resumo (alterados/zerados/novos) e chame `notifyDailySummary` ao final (D-69). Preserve `seedPendingApprovalQueue` (a fila de aprovação vira histórico/verificação opcional) e a disciplina de processo: `runDailyJob` NUNCA fecha a conexão nem chama `process.exit` — só o bloco CLI (via `checkpointAndCloseDb`).
  </action>
  <acceptance_criteria>
    - Teste (mocks): com kill switch off (dryRun true), `executeScheduledWrite` é chamado com `dryRun:true` para os produtos com diff e NENHUMA escrita real acontece; o resumo diário é enviado.
    - Teste (mocks): com kill switch on e sem disjuntor disparado, `executeScheduledWrite` é chamado com `dryRun:false` apenas para fontes elegíveis (`hasAvailableGrade` e `published !== false`) e com diff real; fontes esgotadas/ocultas não recebem escrita de conteúdo (conjunto vazio).
    - Teste (mocks): disjuntor disparado (churn/apagão além do limiar, não-primeiro-rollout) → nenhuma chamada de `executeScheduledWrite` em modo real + `notifyWriteFailure` chamado.
    - Teste de `notifyDailySummary`: sem `WRITE_FAILURE_WEBHOOK_URL` degrada (retorna não-notificado) e NÃO lança; com webhook, faz um único fetch com payload sem credencial.
    - Teste: importar `run-daily-job.js` NÃO dispara rede/DB (guard ESM preservado).
    - `cd app-partners-recomendados && npx vitest run scripts/run-daily-job.test.js src/review/notify-failure.test.js` sai com código 0.
  </acceptance_criteria>
  <verify>
    <automated>cd app-partners-recomendados && npx vitest run scripts/run-daily-job.test.js src/review/notify-failure.test.js</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API pública Nuvemshop → ingestão/orquestrador | Leitura pode vir truncada (rate-limit/paginação) — não confiável quanto a completude. |
| GitHub Actions env/vars → orquestrador | Toggle operacional (WRITE_ENABLED/WRITE_OVERRIDE/FIRST_ROLLOUT) vive fora do git, na UI do CI. |
| orquestrador → loja (escrita em massa) | Superfície crítica: escrita automática sem gate humano prévio (D-61). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Tampering / DoS de negócio | Leitura truncada (categoria com 0 / total colapsado) interpretada como "produtos sumiram → apagar recomendações" | mitigate | Defesa 1 (D-66) aborta o run e notifica antes de qualquer escrita; disjuntor (D-63) como segunda rede (apagão > 10%). |
| T-07-14 | Tampering / DoS de negócio | Escrita em massa incorreta (churn/apagão) por bug ou dado anômalo | mitigate | `tripBreaker` aborta+notifica; 1º rollout isento apenas via flag explícito FIRST_ROLLOUT. |
| T-07-15 | Elevation of Privilege | Regime automático gravar sem freio operacional | mitigate | Kill switch `WRITE_ENABLED` (ausente=dry-run) + input `write` do dispatch + botão "Disable workflow" nativo; dryRun passado a cada escrita. |
| T-07-16 | Information Disclosure | Webhook do resumo/falha vazar credencial | mitigate | `notifyDailySummary`/`notifyWriteFailure` só enviam productId/erro/resumo/timestamp; nunca token (V7, T-05-01). |
| T-07-17 | Integrity | Perda de escrita em WAL no CI efêmero | mitigate | `checkpointAndCloseDb()` como última operação do bloco CLI (D-45), inalterado. |
| T-07-22 | Information Disclosure / Elevation of Privilege | Secret `NUVEMSHOP_ACCESS_TOKEN` comprometido (vazou em texto puro em sessão anterior — pendência STATE.md) usado pelo regime diário para escrever no catálogo real | mitigate | Regenerar o Access Token no Nuvemshop Partners Portal e atualizar `secrets.NUVEMSHOP_ACCESS_TOKEN` no GitHub antes de habilitar o regime automático de escrita; botão nativo **"Disable workflow"** (Settings > Actions > daily-recompute) como parada dura que impede o cron de disparar com credencial comprometida. Token nunca logado no payload de notificação (V7, T-07-16). |
| T-07-SC | Tampering | Instalação de pacote (supply chain) | accept | Nenhuma dependência nova (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

## Artefatos que esta fase produz

- `run-daily-job.js`: `resolveWriteEnabled()`; sequência estendida de `runDailyJob` (Defesa 1 → cálculo → montagem de `toWrite` só-diff/só-elegível → disjuntor → escrita `executeScheduledWrite` com dryRun → resumo diário); leitura de `FIRST_ROLLOUT`.
- `src/review/circuit-breaker.js` (novo): `tripBreaker({ toWrite, baseline, isFirstRollout, churnMax, blackoutMax })`, `setsEqual(a, b)` — puro, zero-import.
- `src/review/notify-failure.js`: `notifyDailySummary({ summary })` (D-69).
- `.github/workflows/daily-recompute.yml`: inputs `write`/`first_rollout`; env `WRITE_ENABLED`/`WRITE_OVERRIDE`/`FIRST_ROLLOUT`.
- Config runtime nova: repository variable `WRITE_ENABLED` (documentada em user_setup).

<verification>
- `cd app-partners-recomendados && npx vitest run scripts/run-daily-job.test.js src/review/circuit-breaker.test.js src/review/notify-failure.test.js` verde.
- `circuit-breaker.js` zero-import: `grep -cE '^import ' app-partners-recomendados/src/review/circuit-breaker.js` == 0.
- Manual (Plano 07-08): dry-run real do job confere kill switch/Defesa 1/disjuntor contra a loja.
</verification>

<success_criteria>
- Job diário grava automaticamente só produtos elegíveis com diff (D-54/D-61/D-68), com dryRun controlado pelo kill switch (D-62).
- Defesa 1 (D-66) e disjuntor (D-63) abortam+notificam antes de escrita perigosa; 1º rollout isento por flag.
- Resumo diário enviado (D-69); 11 categorias via fonte única (D-53); disciplina de processo/WAL preservada.
</success_criteria>

<output>
Create `.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-05-SUMMARY.md` when done
</output>
