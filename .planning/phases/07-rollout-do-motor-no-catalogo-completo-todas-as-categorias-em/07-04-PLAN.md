---
phase: 07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app-partners-recomendados/scripts/rollback.js
  - app-partners-recomendados/scripts/rollback.test.js
  - app-partners-recomendados/scripts/rollback-batch.js
  - app-partners-recomendados/scripts/rollback-batch.test.js
autonomous: true
requirements: [WRTE-03]
must_haves:
  truths:
    - "Um segundo rollback consecutivo sobre um produto cujo Metafield já foi deletado NÃO lança TypeError (CR-01 corrigido) (D-65)"
    - "Quando o Metafield não existe mais e há valor a restaurar, o rollback o RECRIA via createMetafield (não updateMetafield sobre null) (D-65)"
    - "Quando o Metafield não existe e não há valor a restaurar, o rollback é no-op registrado, sem dereferenciar existing.id (D-65)"
    - "Existe um rollback em lote que itera produtos chamando performRollback e AGREGA falhas/conflitos sem abortar o lote inteiro (D-65)"
    - "RollbackConflictError (D-38) continua abortando o rollback de um produto sem sobrescrever edição mais recente (preservado)"
  artifacts:
    - path: "app-partners-recomendados/scripts/rollback.js"
      provides: "performRollback com guarda existing == null (CR-01)"
      contains: "createMetafield"
    - path: "app-partners-recomendados/scripts/rollback-batch.js"
      provides: "CLI de rollback em lote sobre write_log, agrega resultados"
      contains: "performRollback"
  key_links:
    - from: "app-partners-recomendados/scripts/rollback-batch.js"
      to: "app-partners-recomendados/scripts/rollback.js (performRollback)"
      via: "reuso por-produto (nunca reescreve a lógica de rollback, D-38)"
      pattern: "performRollback"
---

<objective>
Tornar o rollback seguro em **escala de catálogo**: (1) corrigir o bug conhecido **CR-01** (null-pointer no rollback duplo sobre Metafield já deletado) em `rollback.js`; (2) criar um **rollback em lote** (`rollback-batch.js`) que reusa `performRollback` por produto e agrega falhas sem abortar — a rede de segurança que torna a escrita automática (Opção B, D-61) operacionalmente aceitável (D-65).

Purpose: o 1º rollout supervisionado (Plano 07-08) e o regime diário precisam de um desfazer confiável em massa antes de qualquer escrita de catálogo completo.
Output: `performRollback` com guarda `existing == null`; `scripts/rollback-batch.js` (CLI) agregando resultados; testes cobrindo rollback duplo (Test 7) e agregação de lote.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-CONTEXT.md
@.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-RESEARCH.md
@.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Correção CR-01 — guarda existing == null em performRollback (D-65)</name>
  <files>app-partners-recomendados/scripts/rollback.js, app-partners-recomendados/scripts/rollback.test.js</files>
  <read_first>
    - app-partners-recomendados/scripts/rollback.js (todo o arquivo: `performRollback` l.51-83; guarda de conflito l.60-62; `restoredValue` l.64; dereferências de `existing.id` l.67/73; import de client l.20)
    - app-partners-recomendados/scripts/rollback.test.js (cabeçalho l.1-30 descreve Tests 1-6; padrão vi.mock de client.js + CATALOG_DB_DIR + insertWriteLog real para semear write_log)
    - .planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-RESEARCH.md §"Pitfall 1" e §"Code Examples > Correção mínima de CR-01" (root cause linha a linha)
  </read_first>
  <action>
    Escreva o teste primeiro (RED — deve reproduzir o TypeError atual antes da correção). Adicione `createMetafield` ao import de `../src/nuvemshop-client/client.js`. Após a guarda de conflito existente (que já preserva `RollbackConflictError`, D-38), trate `existing == null` explicitamente: se `restoredValue == null`, é no-op (nada a restaurar, Metafield já ausente) — registre a linha de rollback em `write_log` com `metafieldId: null` e não chame nenhuma função de rede; se `restoredValue != null`, o Metafield precisa ser RECRIADO via `createMetafield({ ownerId: productId, value: restoredValue })` (não `updateMetafield`, que dereferenciaria `existing.id` nulo). No `insertWriteLog`, use `metafieldId: existing ? existing.id : (result && result.id) || null` — nunca `existing.id` cru. Preserve o idioma ESM CLI-only (l.88-100) e a `RollbackConflictError` inalterados.
  </action>
  <acceptance_criteria>
    - Test 7 novo: rollback duplo sobre um produto cujo 1º rollback deletou o Metafield (write_log tem uma linha rollback com `writtenValue` null) NÃO lança; no 2º rollback, com `restoredValue` não-nulo e `findMetafield` retornando null, `createMetafield` É chamado e `updateMetafield`/`deleteMetafield` NÃO.
    - Teste no-op: `existing` null e `restoredValue` null → nenhuma chamada de rede, uma linha de rollback com `metafieldId: null` registrada.
    - Tests 1-6 existentes continuam verdes (RollbackConflictError, delete simples, guard CLI).
    - `cd app-partners-recomendados && npx vitest run scripts/rollback.test.js` sai com código 0.
  </acceptance_criteria>
  <verify>
    <automated>cd app-partners-recomendados && npx vitest run scripts/rollback.test.js</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rollback-batch.js — rollback em lote com agregação (D-65)</name>
  <files>app-partners-recomendados/scripts/rollback-batch.js, app-partners-recomendados/scripts/rollback-batch.test.js</files>
  <read_first>
    - app-partners-recomendados/scripts/rollback.js (idioma ESM CLI-only l.85-101; `performRollback`/`RollbackConflictError` — a serem reusados, nunca reescritos)
    - app-partners-recomendados/src/db/catalog-store.js (`listWriteLog()` l.490-492 — fonte de product_ids com escrita; `checkpointAndCloseDb` l.590-593 para o bloco CLI)
    - app-partners-recomendados/scripts/rollback.test.js (padrão de isolamento vi.mock + CATALOG_DB_DIR para o teste do lote)
    - .planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-PATTERNS.md §"rollback-batch.js"
  </read_first>
  <action>
    Escreva os testes primeiro (RED). Crie `scripts/rollback-batch.js` com uma função exportada `performBatchRollback({ runId } = {})`: determina o conjunto de produtos-alvo a partir de `listWriteLog()` — dedupe por `productId` das linhas com `status === 'success'`, filtrando por `runId` quando informado (senão todos os produtos com escrita success) — e, para cada produto, chama `performRollback({ productId })` importado de `rollback.js`, capturando o resultado num acumulador que NUNCA aborta o lote: cada item vira `{ productId, outcome: 'reverted' | 'conflict' | 'error' | 'noop', message? }` (conflito = `RollbackConflictError`, erro = qualquer outra exceção). Retorna `{ total, reverted, conflicts, errors, items }`. A função NUNCA fecha a conexão nem chama `process.exit` (mesma disciplina de `run-daily-job`/`rollback`). No bloco ESM CLI-only (`import.meta.url === pathToFileURL(process.argv[1]).href`), aceite `--run <id>` opcional, imprima o resumo agregado, chame `checkpointAndCloseDb()` e `process.exit(errors > 0 ? 1 : 0)`. Só é seguro após a correção CR-01 (Task 1).
  </action>
  <acceptance_criteria>
    - Teste: lote com 3 produtos em que 1 dá `RollbackConflictError` e 1 lança erro genérico ainda processa o 3º e retorna `total:3, reverted:1, conflicts:1, errors:1` (não aborta no primeiro problema).
    - Teste: `--run <id>` restringe o lote aos produtos com escrita success daquele run_id.
    - Teste: importar o módulo NÃO executa o corpo do CLI (guard ESM) nem toca a rede.
    - `cd app-partners-recomendados && npx vitest run scripts/rollback-batch.test.js` sai com código 0.
  </acceptance_criteria>
  <verify>
    <automated>cd app-partners-recomendados && npx vitest run scripts/rollback-batch.test.js</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI operador → rollback em lote → API Nuvemshop | Ação destrutiva em massa; cada produto revalida divergência ao vivo (D-38) antes de qualquer efeito. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Denial of Service (batch travar) | `performRollback` lançando TypeError (CR-01) no meio de um lote | mitigate | Guarda `existing == null` + `createMetafield` para recriar; Test 7 prova que rollback duplo não lança. |
| T-07-11 | Tampering | Rollback em lote sobrescrever edição manual mais recente no admin | mitigate | `RollbackConflictError` (D-38) aborta o produto divergente; o lote agrega o conflito e segue, nunca força escrita. |
| T-07-12 | Integrity | Lote abortar no primeiro erro deixando catálogo em estado parcial inconsistente | mitigate | Acumulador nunca aborta; resultado por produto (`reverted/conflict/error/noop`) + exit code refletindo erros. |
| T-07-SC | Tampering | Instalação de pacote (supply chain) | accept | Nenhuma dependência nova (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

## Artefatos que esta fase produz

- `rollback.js`: `performRollback` com guarda `existing == null` (recria via `createMetafield` ou no-op); import de `createMetafield`.
- `scripts/rollback-batch.js` (novo): `performBatchRollback({ runId })` exportada + bloco CLI com flag `--run <id>`; retorno agregado `{ total, reverted, conflicts, errors, items }`.
- `rollback.test.js`: Test 7 (rollback duplo não lança) + caso no-op.

<verification>
- `cd app-partners-recomendados && npx vitest run scripts/rollback.test.js scripts/rollback-batch.test.js` verde.
</verification>

<success_criteria>
- CR-01 corrigido: rollback duplo não lança; recria/no-op corretos (D-65).
- Rollback em lote agrega falhas sem abortar; reusa `performRollback` (D-65/D-38).
- Nenhuma regressão nos testes de rollback existentes.
</success_criteria>

<output>
Create `.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-04-SUMMARY.md` when done
</output>
