---
phase: 07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em
plan: 08
type: execute
wave: 4
depends_on: ["07-04", "07-05", "07-06"]
files_modified: []
autonomous: false
requirements: [RULE-01, APRV-03]
user_setup:
  - service: nuvemshop-partners-portal
    why: "O NUVEMSHOP_ACCESS_TOKEN apareceu em texto puro numa sessão anterior (pendência de segurança do STATE.md) — precisa ser regenerado ANTES de qualquer escrita real do rollout (D-64)"
    dashboard_config:
      - task: "Regenerar o Access Token do app (invalida o token vazado)"
        location: "Nuvemshop Partners Portal > App > Credenciais / Access Token"
  - service: github-actions
    why: "Atualizar o secret com o token regenerado, confirmar o webhook de notificação e o kill switch antes do run real (D-62/D-63/D-64)"
    env_vars:
      - name: NUVEMSHOP_ACCESS_TOKEN
        source: "Partners Portal (token regenerado) -> GitHub > Settings > Secrets and variables > Actions > Secrets"
      - name: WRITE_FAILURE_WEBHOOK_URL
        source: "Webhook do disjuntor/resumo diário (confirmar configurado; ausente degrada para log local)"
    dashboard_config:
      - task: "Confirmar/criar a repository variable WRITE_ENABLED (ausente/false => dry-run seguro)"
        location: "GitHub > Settings > Secrets and variables > Actions > Variables"
must_haves:
  truths:
    - "PRÉ-CONDIÇÃO: o NUVEMSHOP_ACCESS_TOKEN vazado foi regenerado no Partners Portal e o secret no GitHub atualizado ANTES de qualquer escrita real do run inicial (D-64, pendência STATE.md)"
    - "O 1º rollout completo roda em DRY-RUN primeiro (kill switch off — WRITE_ENABLED/input write=false), sem nenhuma escrita real na loja (D-62/D-64)"
    - "A cobertura do run inicial é conferida pelo relatório de cobertura (Plano 07-06) MAIS o GET /audit (Fase 5) antes de habilitar a escrita (D-60/D-64)"
    - "Só após a conferência humana a escrita real do run inicial é habilitada (input write=true), fechando o item (1) do GOAL da fase — o rollout REAL supervisionado das 11 categorias, só fontes com estoque e visíveis (D-53/D-54/D-64)"
    - "O 1º run é ISENTO do disjuntor via flag FIRST_ROLLOUT=true (D-63 exceção — baseline vazio muda ~100% por natureza); a isenção vem do workflow_dispatch (não persistente) e o disjuntor volta a valer no regime diário automático (D-63/D-68)"
  artifacts:
    - path: ".planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-08-SUMMARY.md"
      provides: "registro operacional do rollout supervisionado: token regenerado, dry-run conferido, cobertura, escrita habilitada, /audit pós-escrita"
  key_links:
    - from: "07-08 (rollout supervisionado)"
      to: "app-partners-recomendados/scripts/run-daily-job.js (resolveWriteEnabled / FIRST_ROLLOUT)"
      via: "workflow_dispatch inputs write + first_rollout (Plano 07-05) controlam dry-run->escrita e a isenção do disjuntor"
      pattern: "FIRST_ROLLOUT"
    - from: "07-08 (conferência)"
      to: "app-partners-recomendados/scripts/coverage-report.js (Plano 07-06)"
      via: "relatório de cobertura usado na conferência humana do dry-run antes de habilitar a escrita"
      pattern: "coverage-report"
---

<objective>
Operar o **primeiro rollout completo supervisionado** (D-64) — o item (1) do GOAL desta fase: o rollout REAL do motor sobre as **11 categorias da taxonomia** (D-53), só para produtos-fonte **com estoque e visíveis** (D-54/D-58), pelo caminho de escrita automática (`scheduled`, D-61) construído nos Planos 07-03/07-05. Como o portão de aprovação prévia foi aposentado (D-61), este PRIMEIRO run é conduzido manualmente e em etapas: (a) regenera-se o token vazado (pré-condição de segurança); (b) roda-se em **dry-run primeiro** (D-62); (c) confere-se a cobertura (Plano 07-06) + `GET /audit` (Fase 5); (d) só então habilita-se a **escrita real** desse run inicial, (e) com o **disjuntor isento** apenas neste run via `FIRST_ROLLOUT` (D-63 exceção). O regime diário automático (D-68) assume a partir daqui, já com o disjuntor ativo.

Purpose: fechar o rollout com segurança operacional — nenhuma escrita em massa sem token válido, sem dry-run conferido e sem supervisão humana. A partir daí o ciclo diário roda sozinho, guardado por kill switch (D-62), disjuntor (D-63) e Defesas 1/2 (D-66/D-67).
Output: as 11 categorias com Metafields gravados (fontes elegíveis), registrados em `write_log` com `triggered_by='scheduled'`; `07-08-SUMMARY.md` documentando o rollout supervisionado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-CONTEXT.md
@.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-RESEARCH.md
@.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-05-PLAN.md
@.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-06-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Pré-condição de segurança — regenerar o token vazado e configurar o CI (D-64)</name>
  <what-built>
    Nada de código — esta é a pré-condição operacional obrigatória antes de qualquer escrita real.
    O `NUVEMSHOP_ACCESS_TOKEN` apareceu em texto puro numa sessão anterior (pendência de segurança do STATE.md);
    habilitar o motor a gravar em escala no catálogo real com uma credencial comprometida é inaceitável (T-07-22).
  </what-built>
  <action>
    Tarefa de checkpoint humano (sem código). O executor apresenta os 5 passos de pré-condição ao operador e AGUARDA o sinal antes de prosseguir; não dispara NENHUM run com escrita real enquanto o token não for confirmado como regenerado — é bloqueio duro (T-07-22, gate blocking-human). Os passos estão em `<how-to-verify>`.
  </action>
  <how-to-verify>
    1. No Nuvemshop Partners Portal, regenere o Access Token do app (isso invalida o token vazado).
    2. Atualize o secret `NUVEMSHOP_ACCESS_TOKEN` em GitHub > Settings > Secrets and variables > Actions > Secrets com o novo token.
    3. Confirme que o secret `WRITE_FAILURE_WEBHOOK_URL` está configurado (senão o disjuntor D-63 e o resumo diário D-69 degradam silenciosamente para log local).
    4. Confirme/crie a repository variable `WRITE_ENABLED` (ausente ou "false" => dry-run seguro por padrão, D-62).
    5. Confirme que o botão nativo "Disable workflow" (Settings > Actions > daily-recompute) está disponível como parada dura.
  </how-to-verify>
  <resume-signal>Digite "token-regenerado" após concluir os 5 passos, ou descreva o bloqueio.</resume-signal>
  <done>Token regenerado no Partners Portal e secret atualizado; webhook e WRITE_ENABLED confirmados; "Disable workflow" disponível — nenhuma escrita real ocorreu ainda.</done>
</task>

<task type="auto">
  <name>Task 2: Run inicial em DRY-RUN das 11 categorias + relatório de cobertura (D-62/D-64)</name>
  <files>Nenhum arquivo de código alterado — operação supervisionada sobre o pipeline dos Planos 07-01..07-06.</files>
  <read_first>
    - app-partners-recomendados/scripts/run-daily-job.js (`resolveWriteEnabled` + leitura de `FIRST_ROLLOUT` — Plano 07-05)
    - app-partners-recomendados/scripts/coverage-report.js (CLI read-only do relatório de cobertura — Plano 07-06)
    - .github/workflows/daily-recompute.yml (inputs `write`/`first_rollout` e env `WRITE_ENABLED`/`WRITE_OVERRIDE`/`FIRST_ROLLOUT` — Plano 07-05)
    - .planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-RESEARCH.md §"Pattern 3" (kill switch: dry-run primeiro) e §"Environment Availability" (token obrigatório)
  </read_first>
  <action>
    Com a pré-condição (Task 1) concluída, dispare o run inicial completo das 11 categorias em modo DRY-RUN — kill switch off (`WRITE_ENABLED` ausente/"false" ou input `write=false`) e `FIRST_ROLLOUT=true` (isenção do disjuntor, D-63) — via o `workflow_dispatch` de `daily-recompute` (input `write=false`, `first_rollout=true`) OU localmente com `WRITE_ENABLED` não definido. O job deve INGERIR as 11 categorias (persistindo `published`, D-58) e CALCULAR o que gravaria, logando as escritas pretendidas SEM nenhuma escrita real (`dryRun=true` a cada escrita, reusando APRV-04). Em seguida, gere o relatório de cobertura executando o CLI read-only `coverage-report.js` sobre o snapshot recém-ingerido. NÃO habilite a escrita real neste passo. Registre no SUMMARY: total de fontes com estoque, cobertos, zeradas com motivo (oculta / sem cor / sem par mesma-cor-em-estoque) e a lista de reprocesso (fontes sem tecido canônico).
  </action>
  <acceptance_criteria>
    - O run em dry-run conclui sem erro e NÃO cria nenhuma linha em `write_log` com `triggered_by='scheduled'` e `status='success'` (nenhuma escrita real ocorreu).
    - O relatório de cobertura é produzido com `totalSourcesInStock`, `covered`, `zeroed` (com motivo) e `reprocess`.
    - A Defesa 1 (D-66) NÃO abortou por leitura truncada: as 11 categorias voltaram com produtos e o total está dentro da banda vs. o último run — caso contrário, investigar antes de prosseguir (não é para habilitar escrita).
  </acceptance_criteria>
  <verify>
    <automated>cd app-partners-recomendados && node scripts/coverage-report.js</automated>
  </verify>
  <done>Dry-run das 11 categorias concluído sem escrita real (nenhuma linha `scheduled`/`success` nova em `write_log`); relatório de cobertura produzido com totais/cobertos/zeradas-com-motivo/reprocesso; Defesa 1 não abortou.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Conferência do dry-run — relatório de cobertura + GET /audit (D-60/D-64)</name>
  <what-built>
    O run inicial rodou em dry-run e produziu o relatório de cobertura (Task 2). Antes de habilitar
    a escrita real, o operador confere se a cobertura e o conjunto de escritas pretendidas fazem sentido.
  </what-built>
  <action>
    Tarefa de checkpoint humano (sem código). O executor apresenta o relatório de cobertura do dry-run (Task 2) e o `GET /audit` ao operador e AGUARDA a decisão; não habilita a escrita real sem o sinal de aprovação. Se houver divergência (cobertura implausível, zeradas com motivo inesperado), o operador corrige antes (reingerir, taguear tecido, desmarcar "Oculto") e repete o dry-run. Os passos de conferência estão em `<how-to-verify>`.
  </action>
  <how-to-verify>
    1. Revise o relatório de cobertura (`coverage-report.js`): a manchete de cobertura (informativa, sem meta fixa — D-59) é plausível? As zeradas têm motivo coerente (oculta / sem cor / sem par mesma-cor-em-estoque no grupo elegível)?
    2. A lista de reprocesso (fontes sem tecido canônico a taguear e rerodar) está compatível com o esperado?
    3. Suba o painel e consulte o `GET /audit` (Fase 5) para conferir o histórico e o diff antes/depois pretendido.
    4. Confirme que nenhuma escrita real ocorreu ainda (`write_log` sem novas linhas `scheduled`/`success`).
  </how-to-verify>
  <resume-signal>Digite "cobertura-ok" para autorizar a escrita real do run inicial, ou descreva as divergências a corrigir primeiro (reingerir, taguear tecido, desmarcar "Oculto", etc.).</resume-signal>
  <done>Relatório de cobertura e `GET /audit` conferidos pelo operador; nenhuma escrita real ocorreu; decisão de prosseguir (ou corrigir e repetir o dry-run) registrada.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Habilitar a escrita real do run inicial (disjuntor isento) + conferir /audit pós-escrita (D-63/D-64)</name>
  <what-built>
    Com a cobertura aprovada (Task 3), o run inicial é reexecutado COM escrita real, isento do disjuntor
    apenas neste run (`FIRST_ROLLOUT=true`, D-63 exceção — baseline vazio muda ~100% por natureza).
    É o item (1) do GOAL da fase: o rollout REAL supervisionado das 11 categorias.
  </what-built>
  <action>
    Tarefa de checkpoint humano. Após a aprovação da cobertura (Task 3), o operador dispara o `workflow_dispatch` de `daily-recompute` com `write=true` e `first_rollout=true` (escrita real + disjuntor isento SÓ neste run), confere o `/audit` pós-escrita, e confirma que a isenção do disjuntor NÃO ficou persistente (`first_rollout` é input do dispatch, nunca uma repository variable). Deixa `WRITE_ENABLED` no estado desejado para o regime diário. AGUARDA o sinal de conclusão. Os passos estão em `<how-to-verify>`.
  </action>
  <how-to-verify>
    1. Dispare o `workflow_dispatch` de `daily-recompute` com input `write=true` e `first_rollout=true` (escrita real + disjuntor isento SÓ neste run).
    2. Acompanhe o run: as escritas gravam Metafields via o caminho `scheduled` (`triggered_by='scheduled'`), com a Defesa 2 (D-67) descartando ids inválidos e conjuntos vazios virando lacuna registrada (nunca lixo gravado).
    3. Consulte o `GET /audit` e o `write_log`: as escritas do run inicial estão registradas (WRTE-04); nenhuma falha não tratada.
    4. Confirme que a isenção do disjuntor NÃO ficou persistente: `first_rollout` é input do `workflow_dispatch` (não uma repository variable), então o cron diário volta a rodar com o disjuntor ATIVO (D-63) e o regime automático (D-68) assume a partir daqui.
    5. Deixe a repository variable `WRITE_ENABLED` no estado desejado para o regime diário (true para gravar automaticamente todo dia às 03h, D-52).
  </how-to-verify>
  <resume-signal>Digite "rollout-concluido" após confirmar as escritas no `/audit` e a volta do disjuntor ao regime diário, ou descreva o problema (usar `rollback-batch.js` do Plano 07-04 para desfazer em lote se necessário).</resume-signal>
  <done>Escrita real do run inicial concluída e conferida no `/audit`/`write_log` (linhas `scheduled`); disjuntor de volta ao regime diário (isenção não-persistente); `WRITE_ENABLED` no estado desejado — item (1) do GOAL entregue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador -> workflow_dispatch (write/first_rollout) -> loja | Escrita em massa no 1º run com o disjuntor ISENTO — a supervisão humana (dry-run + conferência) é o único freio antes de habilitar. |
| secret NUVEMSHOP_ACCESS_TOKEN -> API pública Nuvemshop | Token OAuth escreve no catálogo real; permanece comprometido até ser regenerado (pendência STATE.md). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-22 | Information Disclosure / Elevation of Privilege | Secret `NUVEMSHOP_ACCESS_TOKEN` comprometido (vazou — STATE.md) usado para escrever no catálogo real | mitigate | Task 1 BLOQUEIA o run real até o token ser regenerado no Partners Portal e o secret atualizado no GitHub; botão "Disable workflow" como parada dura; token nunca logado (V7). |
| T-07-23 | Tampering / DoS de negócio | 1º rollout com disjuntor ISENTO (`FIRST_ROLLOUT`) deixar passar apagão/churn em massa indevido | mitigate | Dry-run obrigatório primeiro (Task 2) + conferência humana de cobertura + `GET /audit` (Task 3) ANTES de habilitar a escrita (Task 4); rollback em lote (`rollback-batch.js`, Plano 07-04) como desfazer. |
| T-07-24 | Elevation of Privilege | Isenção do disjuntor (`FIRST_ROLLOUT`) permanecer ligada e desarmar o freio do regime diário automático | mitigate | `FIRST_ROLLOUT` é input do `workflow_dispatch` (não persistente, nunca uma repository variable); o cron diário volta com o disjuntor ATIVO (D-63); Task 4 confirma explicitamente a volta ao regime diário. |
| T-07-SC | Tampering | Instalação de pacote (supply chain) | accept | Nenhuma dependência nova (RESEARCH §Package Legitimacy Audit: N/A); nenhum `npm install`. |
</threat_model>

## Artefatos que esta fase produz

- Nenhum arquivo de código novo — esta fase OPERA de forma supervisionada o pipeline dos Planos 07-01..07-06.
- Efeito no mundo real: Metafields `recomendados.produto_sugerido` gravados nas 11 categorias (só produtos-fonte com estoque e visíveis, D-54/D-58), registrados em `write_log` com `triggered_by='scheduled'`.
- `07-08-SUMMARY.md`: registro operacional do rollout supervisionado (token regenerado; cobertura do dry-run com totais/cobertos/zeradas-com-motivo/reprocesso; escrita habilitada; conferência pós-escrita no `/audit`; confirmação de que o disjuntor voltou ao regime diário).

<verification>
- Task 2 (automated): `cd app-partners-recomendados && node scripts/coverage-report.js` produz o relatório de cobertura contra o snapshot do dry-run; nenhuma escrita real (`write_log` sem novas linhas `scheduled`/`success`).
- Task 3 (manual): conferência humana de cobertura + `GET /audit` antes de habilitar.
- Task 4 (manual): escrita real habilitada e conferida no `/audit`; disjuntor volta ao regime diário (isenção não-persistente).
</verification>

<success_criteria>
- Item (1) do GOAL entregue: rollout REAL supervisionado das 11 categorias, só fontes com estoque e visíveis (D-53/D-54/D-64).
- Pré-condição de segurança satisfeita (token regenerado) ANTES de qualquer escrita real (D-64/T-07-22).
- Dry-run conferido (cobertura + `/audit`) antes da escrita (D-60/D-62/D-64); disjuntor isento SÓ no 1º run via `FIRST_ROLLOUT` não-persistente; regime automático assume com o disjuntor ativo (D-63/D-68).
</success_criteria>

<output>
Create `.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-08-SUMMARY.md` when done
</output>
