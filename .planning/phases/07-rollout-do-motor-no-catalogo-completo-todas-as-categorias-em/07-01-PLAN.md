---
phase: 07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app-partners-recomendados/src/recommendation/recommendation-engine.js
  - app-partners-recomendados/src/recommendation/recommendation-engine.test.js
autonomous: true
requirements: [RULE-01, RULE-02]
must_haves:
  truths:
    - "Um candidato de peso 1 (Estoque+Cor+Tecido, E+C+T) sempre aparece acima de um candidato de peso 2 (Estoque+Cor, E+C) na mesma cota, mesmo com estoque total menor (D-55/D-56)"
    - "Um candidato de tecido diferente da fonte (ambos com tecido canônico) passa a ser elegível como peso 2, não mais excluído (D-55/D-57)"
    - "Um candidato com published === false nunca é recomendado (D-58)"
    - "Uma fonte com published === false não gera nenhuma recomendação — retorna [] (D-58)"
    - "published === null ou undefined (produto pré-migração) NÃO é tratado como oculto (D-58, Pitfall 2)"
    - "Cor permanece sempre obrigatória e Look Inteiro permanece auto-contido; toda a composição de cota 4+4 e backfill da Fase 03.1 fica intacta (D-57/D-27/RULE-01)"
  artifacts:
    - path: "app-partners-recomendados/src/recommendation/recommendation-engine.js"
      provides: "motor puro com modelo de 2 pesos e consumo do flag published, zero-import (RULE-02)"
      contains: "candidateWeight"
    - path: "app-partners-recomendados/src/recommendation/recommendation-engine.test.js"
      provides: "cobertura do modelo de 2 pesos (D-55/D-56/D-57) e da visibilidade (D-58)"
  key_links:
    - from: "app-partners-recomendados/src/recommendation/recommendation-engine.js"
      to: "getLatestSnapshotProducts (catalog-store.js, Plano 07-02)"
      via: "propriedade booleana published no CatalogProductEntry, consumida pronta sem I/O"
      pattern: "published === false"
---

<objective>
Substituir o override de 2026-07-17 no motor puro pelo **modelo de 2 pesos (D-55/D-56/D-57)** e adicionar o **consumo do flag de visibilidade `published` (D-58)** — mantendo o motor `recommendation-engine.js` 100% puro (zero-import, zero-I/O, RULE-02) e preservando integralmente a lógica de Grupo de Produtos / cota 4+4 / backfill simétrico da Fase 03.1 (D-26 a D-35).

Purpose: elevar a cobertura (tecido diferente deixa de excluir, vira peso 2 de backfill) com ranking explícito por qualidade de match (E+C+T acima de E+C), e eliminar o bug real de recomendar produto oculto → 404 (D-58), sem quebrar RULE-01/RULE-02.
Output: motor com `candidateWeight`, `isEligibleCandidateInGroup`/`buildSortedPool` refatorados, guarda de fonte oculta em `recommendForProduct`, typedef `CatalogProductEntry` com `published`, e testes atualizados/novos verdes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-CONTEXT.md
@.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-RESEARCH.md
@.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Modelo de 2 pesos (D-55/D-56/D-57) em recommendation-engine.js</name>
  <files>app-partners-recomendados/src/recommendation/recommendation-engine.js, app-partners-recomendados/src/recommendation/recommendation-engine.test.js</files>
  <read_first>
    - app-partners-recomendados/src/recommendation/recommendation-engine.js (estado atual: `isEligibleCandidateInGroup` l.144-163 com o bloco de tecido do override l.151-160; `buildSortedPool` l.176-181; `recommendForProduct` l.355-399; typedef `CatalogProductEntry` l.52-61)
    - app-partners-recomendados/src/recommendation/recommendation-engine.test.js (Test com tecido diferente ~l.170 que muda de assertion; Tests 20/21/27/28 de grupo a re-verificar; factory `makeProduct` l.27-45)
    - .planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-RESEARCH.md §"Pattern 2" (design dos 2 pesos + invariante crítica D-56)
    - .planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-PATTERNS.md §"recommendation-engine.js"
  </read_first>
  <action>
    Escreva primeiro os testes (RED), depois implemente (GREEN). Introduza a função interna `candidateWeight(source, candidate, considerFabric)`: retorna 1 quando `considerFabric` é truthy E ambos (fonte e candidato) têm `fabricTagCanonical` não-nulo E os tecidos batem após `normalizeMatchValue`; retorna 2 em qualquer outro caso elegível (inclui tecido diferente, tecido ausente em um lado, e todo o bloco cruzado onde `considerFabric` é falsy). Em `isEligibleCandidateInGroup`, REMOVA o bloco de tecido do override (o `if (considerFabric) { ...bothHaveFabric... return false }`) — tecido nunca mais exclui; o piso de elegibilidade passa a ser Estoque + Cor + mesmo grupo (D-57). Em `buildSortedPool`, mapeie cada candidato elegível para um par contendo a recomendação e seu peso, ordene por `(a.weight - b.weight)` e, em empate de peso, por `compareRecommendations` (cascata D-13 inalterada, D-56 — a partição por peso fica ACIMA de D-13, D-13 ordena DENTRO de cada peso), e devolva só as recomendações. NÃO toque em `composeGroupQuota`, `compareRecommendations`, `GROUP_QUOTA_PER_SIDE`, nem na cota 4+4/backfill simétrico (D-28/D-29 preservados). Reescreva o comentário de cabeçalho e o JSDoc de `isEligibleCandidateInGroup`/`recommendForProduct` para descrever o modelo de 2 pesos (D-55/D-56/D-57), removendo a menção ao override de 2026-07-17 e à reversão de D-15/D-16.
  </action>
  <acceptance_criteria>
    - `cd app-partners-recomendados && npx vitest run src/recommendation/recommendation-engine.test.js` sai com código 0.
    - Existe um teste novo que prova a invariante D-56: uma fonte com tecido 'Viscose', um candidato peso 1 ('Viscose', estoque total 1) e um candidato peso 2 ('Algodao', estoque total 100) no mesmo grupo retornam nesta ordem exata `['<peso1>', '<peso2>']` (peso 1 acima apesar do estoque menor).
    - O teste que antes excluía candidato de tecido diferente (ambos com tecido) agora asserta que ele é INCLUÍDO como peso 2 (assertion passa de dois ids para três ids, com o de tecido diferente por último dentro do empate).
    - `grep -cE '^import ' app-partners-recomendados/src/recommendation/recommendation-engine.js` retorna 0 (RULE-02: motor permanece zero-import).
    - Os 16 testes de regressão de Look Inteiro/cascata (Tests 1-16) e os testes de grupo (Tests 17-29) continuam verdes, sem regressão.
  </acceptance_criteria>
  <verify>
    <automated>cd app-partners-recomendados && npx vitest run src/recommendation/recommendation-engine.test.js</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consumo do flag published (D-58) no motor puro</name>
  <files>app-partners-recomendados/src/recommendation/recommendation-engine.js, app-partners-recomendados/src/recommendation/recommendation-engine.test.js</files>
  <read_first>
    - app-partners-recomendados/src/recommendation/recommendation-engine.js (typedef `CatalogProductEntry` l.52-61 — onde entra `published`; `isEligibleCandidateInGroup` l.144-163; `recommendForProduct` l.355-372, guardas `source.colorValue == null` e `sourceGroup == null`)
    - app-partners-recomendados/src/ingestion/stock-availability.js (padrão do flag persistido `hasAvailableGrade` que `published` espelha)
    - .planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-RESEARCH.md §"Pattern 1" e §"Pitfall 2" (só `=== false` é oculto; `null` nunca)
  </read_first>
  <action>
    Escreva os testes primeiro (RED), depois implemente (GREEN). Adicione ao typedef `CatalogProductEntry` a propriedade booleana `published` (mesma linha conceitual de `hasAvailableGrade`, flag persistido consumido pronto). Em `isEligibleCandidateInGroup`, adicione uma guarda de candidato oculto logo após a checagem de `hasAvailableGrade`: quando `candidate.published === false`, retorne `false` (candidato oculto nunca é recomendado). Em `recommendForProduct`, adicione uma guarda de fonte oculta logo após a checagem `source.colorValue == null`: quando `source.published === false`, retorne `[]` (fonte oculta não gera vitrine), com a mesma disciplina fail-closed da guarda `sourceGroup == null`. CRÍTICO (D-58/Pitfall 2/A6): a comparação é estritamente `=== false` — `published` valendo `null` ou `undefined` (produto ainda não re-ingerido após a migração) NUNCA conta como oculto. Estenda a factory `makeProduct` do arquivo de teste para aceitar `published` com default `true`, sem quebrar os fixtures existentes (que passam a ter `published: true`).
  </action>
  <acceptance_criteria>
    - Teste novo: candidato com `published: false`, mesma cor/tecido/estoque/grupo da fonte, é EXCLUÍDO do resultado.
    - Teste novo: fonte com `published: false` retorna `[]` (sem lançar).
    - Teste novo: candidato com `published: null` e mesma cor+estoque+grupo é INCLUÍDO (não tratado como oculto) — prova de A6/Pitfall 2.
    - `cd app-partners-recomendados && npx vitest run src/recommendation/recommendation-engine.test.js` sai com código 0.
    - `grep -cE '^import ' app-partners-recomendados/src/recommendation/recommendation-engine.js` retorna 0 (motor permanece zero-import, RULE-02).
  </acceptance_criteria>
  <verify>
    <automated>cd app-partners-recomendados && npx vitest run src/recommendation/recommendation-engine.test.js</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot persistido → motor puro | O motor consome flags (`published`, `hasAvailableGrade`, grupo) já resolvidos na ingestão; nunca lê a API nem o banco em runtime (RULE-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure / Integrity | `isEligibleCandidateInGroup` / `recommendForProduct` recomendando produto oculto (link 404) | mitigate | Guarda `candidate.published === false` (candidato) e `source.published === false` (fonte) filtram oculto no motor (D-58); Defesa 2 referencial na escrita (Plano 07-03) reconfere. |
| T-07-02 | Tampering | Zerar catálogo inteiro tratando `published == null` como oculto | mitigate | Comparação estritamente `=== false`; teste dedicado prova que `null`/`undefined` permanece visível (Pitfall 2/A6). |
| T-07-03 | Tampering | Introduzir I/O/import no motor ao adicionar pesos/visibilidade (viola RULE-02) | mitigate | Acceptance criterion `grep -cE '^import '` retorna 0; fixtures in-file, sem rede/DB. |
| T-07-SC | Tampering | Instalação de pacote (supply chain) | accept | Nenhuma dependência nova nesta fase (RESEARCH §Package Legitimacy Audit: N/A); nenhum `npm install` executado. |
</threat_model>

## Artefatos que esta fase produz

- `candidateWeight(source, candidate, considerFabric)` — função interna (não exportada) do motor: peso 1 (E+C+T) / peso 2 (E+C).
- `isEligibleCandidateInGroup` — refatorada: piso E+C, sem exclusão por tecido, com guarda `published === false`.
- `buildSortedPool` — refatorada: ordena por `(peso, cascata D-13)`.
- `recommendForProduct` — nova guarda de fonte oculta (`source.published === false` → `[]`).
- `CatalogProductEntry` typedef — nova propriedade `published` (boolean).
- Novos testes em `recommendation-engine.test.js`: invariante de peso (D-56), candidato/fonte oculto e `published:null` não-oculto (D-58).

<verification>
- Suíte do motor verde: `cd app-partners-recomendados && npx vitest run src/recommendation/recommendation-engine.test.js`.
- Motor permanece zero-import: `grep -cE '^import ' app-partners-recomendados/src/recommendation/recommendation-engine.js` == 0.
- Nenhuma regressão nos Tests 1-29 existentes.
</verification>

<success_criteria>
- Modelo de 2 pesos operante: peso 1 sempre acima de peso 2 (D-56), tecido diferente elegível como peso 2 (D-55/D-57).
- Visibilidade consumida: candidato/fonte `=== false` excluído; `null` não-oculto (D-58).
- RULE-02 preservada (zero-import, zero-I/O); lógica de grupo 03.1 intacta.
</success_criteria>

<output>
Create `.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-01-SUMMARY.md` when done
</output>
