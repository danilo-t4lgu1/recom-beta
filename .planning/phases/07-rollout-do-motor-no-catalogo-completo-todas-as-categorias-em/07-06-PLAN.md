---
phase: 07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em
plan: 06
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - app-partners-recomendados/src/report/coverage-report.js
  - app-partners-recomendados/src/report/coverage-report.test.js
  - app-partners-recomendados/scripts/coverage-report.js
autonomous: true
requirements: [RULE-01]
must_haves:
  truths:
    - "O relatório cobre o catálogo inteiro: total de fontes com estoque, quantas receberam >=1 recomendação (1º ou 2º peso), e o MOTIVO de cada zerada (D-59/D-60)"
    - "Motivo item-a-item das zeradas: fonte oculta, fonte sem cor, ou sem par mesma-cor-em-estoque no grupo elegível (D-60/D-57)"
    - "Um caminho de reprocesso sinaliza produtos-fonte sem tecido canônico para taguear e rerodar (fecha a lacuna que reduz cobertura de 1º peso) (D-60)"
    - "Cobertura NÃO usa % fixo de meta; manchete opcional >=90% é só acompanhamento (D-59)"
    - "buildCoverageReport é puro (recebe o snapshot materializado, chama recommendForProduct) — o CLI faz a leitura de I/O (D-60)"
  artifacts:
    - path: "app-partners-recomendados/src/report/coverage-report.js"
      provides: "buildCoverageReport(catalogProducts) puro: totais, cobertos, zeradas com motivo, reprocesso"
      contains: "buildCoverageReport"
    - path: "app-partners-recomendados/scripts/coverage-report.js"
      provides: "CLI read-only: getLatestSnapshotProducts -> buildCoverageReport -> saída estruturada"
      contains: "getLatestSnapshotProducts"
  key_links:
    - from: "app-partners-recomendados/src/report/coverage-report.js"
      to: "app-partners-recomendados/src/recommendation/recommendation-engine.js (recommendForProduct)"
      via: "conta recommendForProduct(id, snapshot).length > 0 por fonte com estoque"
      pattern: "recommendForProduct"
---

<objective>
Entregar a **validação de cobertura** (D-59/D-60): um relatório diagnóstico que prova, sobre o catálogo inteiro, quantos produtos-fonte com estoque de fato recebem recomendação e, para os que ficam **zerados**, o **motivo** item-a-item — mais um **caminho de reprocesso** que sinaliza fontes sem tecido canônico para o usuário taguear e rerodar (fechando a lacuna que reduz a cobertura de 1º peso).

Purpose: dar visibilidade objetiva do rollout (Plano 07-08 usa este relatório na conferência do 1º rollout supervisionado, D-64), promovendo o protótipo temporário `_scope.js` a código permanente com motivo e reprocesso.
Output: `buildCoverageReport(catalogProducts)` puro + CLI `scripts/coverage-report.js` read-only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-CONTEXT.md
@.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-RESEARCH.md
@.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildCoverageReport puro (D-59/D-60)</name>
  <files>app-partners-recomendados/src/report/coverage-report.js, app-partners-recomendados/src/report/coverage-report.test.js</files>
  <read_first>
    - app-partners-recomendados/scripts/_scope.js (protótipo TEMP l.1-27: já conta por grupo `has_available_grade=1 AND product_group_canonical IS NOT NULL` e `recommendForProduct(...).length > 0`)
    - app-partners-recomendados/src/recommendation/recommendation-engine.js (`recommendForProduct`; shape `CatalogProductEntry` com `published`/`hasAvailableGrade`/`colorValue`/`fabricTagCanonical`/`productGroupCanonical`)
    - .planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-CONTEXT.md §D-59/D-60 (definição de coberto; motivos das zeradas; reprocesso)
    - .planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-PATTERNS.md §"Relatório de cobertura"
  </read_first>
  <action>
    Escreva os testes primeiro (RED). Crie `src/report/coverage-report.js` exportando `buildCoverageReport(catalogProducts)`: itera as fontes com estoque (`hasAvailableGrade` verdadeiro), e para cada uma calcula `recommendForProduct(productId, catalogProducts)`. Uma fonte é COBERTA se o resultado tem length > 0 (1º ou 2º peso — D-59). Para cada fonte ZERADA, determine o motivo com precedência: `published === false` → 'oculta'; `colorValue == null` → 'sem cor'; caso contrário → 'sem par mesma-cor-em-estoque no grupo elegível' (D-60/D-57). Colete também o caminho de REPROCESSO: fontes com estoque cujo `fabricTagCanonical == null` (sinalizadas como 'sem tecido canônico — taguear e rerodar', D-60). Retorne uma estrutura com: `totalSourcesInStock`, `covered`, `zeroed` (array `{ productId, reason }`), `reprocess` (array de productId), agregados por grupo (Look Inteiro / Partes de Cima / Partes de Baixo), e um `headlineCoveragePct` calculado (apenas informativo, sem meta fixa — D-59). Módulo determinístico e sem I/O próprio (recebe o snapshot já materializado); pode importar apenas `recommendForProduct` do motor (mesma disciplina de `diff.js`/`review-queue.js`).
  </action>
  <acceptance_criteria>
    - Teste: fonte oculta (`published:false`) com estoque aparece em `zeroed` com `reason: 'oculta'`.
    - Teste: fonte sem cor (`colorValue:null`) com estoque aparece em `zeroed` com `reason: 'sem cor'`.
    - Teste: fonte com estoque, com cor, sem nenhum par mesma-cor-em-estoque no grupo aparece em `zeroed` com o motivo de grupo.
    - Teste: fonte com estoque e >=1 recomendação conta em `covered` e NÃO em `zeroed`.
    - Teste: fonte com estoque e `fabricTagCanonical:null` aparece em `reprocess`.
    - Teste: `totalSourcesInStock == covered + zeroed.length`; `headlineCoveragePct` coerente (ex.: 3 de 4 → 75).
    - `cd app-partners-recomendados && npx vitest run src/report/coverage-report.test.js` sai com código 0.
  </acceptance_criteria>
  <verify>
    <automated>cd app-partners-recomendados && npx vitest run src/report/coverage-report.test.js</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: CLI read-only scripts/coverage-report.js (D-60)</name>
  <files>app-partners-recomendados/scripts/coverage-report.js</files>
  <read_first>
    - app-partners-recomendados/scripts/run-daily-job.js (idioma ESM CLI-only l.84-109 e `checkpointAndCloseDb` — padrão a replicar; embora este seja read-only, feche a conexão ao final do CLI)
    - app-partners-recomendados/src/db/catalog-store.js (`getLatestSnapshotProducts` l.246; `closeDbForTests`/`checkpointAndCloseDb`)
    - app-partners-recomendados/scripts/_scope.js (protótipo a substituir — mesma leitura de snapshot + motor)
  </read_first>
  <action>
    Crie `scripts/coverage-report.js` como CLI read-only: chama `getLatestSnapshotProducts()`, passa para `buildCoverageReport`, e imprime o resultado em formato estruturado (JSON legível por padrão; aceite uma flag opcional `--csv` que serializa as tabelas de zeradas/reprocesso em CSV — formato à discrição D-60). O corpo do CLI vive atrás do guard ESM (`import.meta.url === pathToFileURL(process.argv[1]).href`); nunca executa ao ser importado. Read-only: não grava nada na loja nem no banco. Este script substitui o protótipo `_scope.js` (removido no Plano 07-07).
  </action>
  <acceptance_criteria>
    - `cd app-partners-recomendados && node scripts/coverage-report.js` roda sem erro e imprime um objeto com `totalSourcesInStock`, `covered`, `zeroed`, `reprocess` (contra o `data/catalog.db` atual — pode refletir o estado real).
    - `grep -c 'getLatestSnapshotProducts' app-partners-recomendados/scripts/coverage-report.js` retorna valor >= 1.
    - `grep -c 'buildCoverageReport' app-partners-recomendados/scripts/coverage-report.js` retorna valor >= 1.
    - Importar o módulo (via um require/import de teste) não dispara o corpo do CLI (guard ESM presente).
  </acceptance_criteria>
  <verify>
    <automated>cd app-partners-recomendados && node scripts/coverage-report.js > /dev/null && echo OK</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite (somente leitura) → relatório | Leitura pura do snapshot; nenhuma escrita na loja ou no banco. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-18 | Information Disclosure | Relatório expor dados sensíveis | accept | Só productId/motivo/contagens de catálogo próprio; sem credenciais, sem PII. |
| T-07-19 | Integrity | Relatório de cobertura enganoso (contar coberto quem não é) mascarar lacuna real | mitigate | `covered` derivado exclusivamente de `recommendForProduct(...).length > 0`; testes fixam totais e motivos. |
| T-07-SC | Tampering | Instalação de pacote (supply chain) | accept | Nenhuma dependência nova (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

## Artefatos que esta fase produz

- `src/report/coverage-report.js` (novo): `buildCoverageReport(catalogProducts)` → `{ totalSourcesInStock, covered, zeroed: [{productId, reason}], reprocess: [productId], byGroup, headlineCoveragePct }`.
- `scripts/coverage-report.js` (novo): CLI read-only com flag opcional `--csv`; substitui o protótipo `_scope.js`.

<verification>
- `cd app-partners-recomendados && npx vitest run src/report/coverage-report.test.js` verde.
- `cd app-partners-recomendados && node scripts/coverage-report.js` produz relatório contra o banco real.
</verification>

<success_criteria>
- Relatório cobre total/cobertos/zeradas-com-motivo + reprocesso, sem meta % fixa (D-59/D-60).
- `buildCoverageReport` puro e determinístico; CLI read-only; `_scope.js` pronto para remoção.
</success_criteria>

<output>
Create `.planning/phases/07-rollout-do-motor-no-catalogo-completo-todas-as-categorias-em/07-06-SUMMARY.md` when done
</output>
