---
phase: 03.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app-partners-recomendados/src/recommendation/recommendation-engine.js
  - app-partners-recomendados/src/recommendation/recommendation-engine.test.js
autonomous: true
requirements: [RULE-01]
must_haves:
  truths:
    - "Um produto-fonte de Look Inteiro nunca mescla com Partes de Cima/Baixo, mesmo que cor+tecido+estoque coincidam com um candidato de outro grupo (D-27)"
    - "Um produto-fonte de Partes de Cima ou Partes de Baixo com >=4 elegíveis em cada lado retorna exatamente 4 do mesmo grupo (cor+tecido+estoque) + 4 do grupo cruzado (cor+estoque, tecido não considerado), bloco mesmo-grupo primeiro no array (D-28/D-35)"
    - "Quando um lado da cota tem menos de 4 elegíveis, o outro lado preenche os slots vazios com backfill simétrico respeitando seu próprio critério, até completar 8 ou esgotar os elegíveis de ambos os lados, nunca inventando candidato inelegível (D-29)"
    - "A cascata de desempate D-13 (estoque total → distribuição → tamanhos centrais) continua decidindo a ordem dentro de cada bloco, sem nenhum nível novo por grupo (D-30, D-14 não reaberto)"
    - "Uma fonte de Partes de Cima/Baixo sem tecido canônico ainda gera o bloco cruzado (cor+estoque), mesmo sem gerar nenhuma recomendação do bloco mesmo-grupo (D-34)"
    - "Um produto-fonte com productGroupCanonical null/desconhecido retorna [] (fail-closed) mesmo que exista um candidato 'irmão' também com grupo null e cor+tecido+estoque idênticos"
    - "Cada Recommendation retornado inclui productGroupCanonical, estendendo o objeto rico D-18 sem remover nenhum campo existente"
    - "Os 16 testes existentes da Fase 3 continuam verdes sem modificação de comportamento para fixtures Look Inteiro, e o motor permanece zero-import/zero-I/O (RULE-02, Test 10 preservado)"
  artifacts:
    - path: "app-partners-recomendados/src/recommendation/recommendation-engine.js"
      provides: "recommendForProduct estendido com critério de Grupo de Produtos (D-26 a D-30, D-34, D-35), Recommendation com productGroupCanonical (D-18 estendido)"
      contains: "function composeGroupQuota"
      min_lines: 100
    - path: "app-partners-recomendados/src/recommendation/recommendation-engine.test.js"
      provides: "cobertura vitest dos 16 comportamentos originais (regressão) + novos comportamentos de grupo/mescla/cota/backfill"
      contains: "GROUP_PARTES_DE_CIMA"
  key_links:
    - from: "app-partners-recomendados/src/recommendation/recommendation-engine.test.js"
      to: "app-partners-recomendados/src/recommendation/recommendation-engine.js"
      via: "import direto das funções/constantes exportadas"
      pattern: "from './recommendation-engine.js'"
---

<objective>
Estender o motor de recomendação determinístico (`recommendForProduct`, Fase 3) com o quarto critério de elegibilidade — Grupo de Produtos (D-26 a D-31, D-33 a D-35 do `03.1-CONTEXT.md`) — preservando 100% o comportamento e os 16 testes já verdes para fixtures de Look Inteiro, e adicionando a mescla com cota fixa 4+4 e backfill simétrico entre Partes de Cima e Partes de Baixo.

Look Inteiro (Vestidos/Macacões/Macaquinhos) permanece auto-contido (D-27): nenhuma mudança de comportamento além de um filtro de grupo explícito adicionado por robustez (hoje inofensivo, pois D-26 confirma que não há produto multi-grupo real — mas correto por construção, não só por sorte de dado). Partes de Cima e Partes de Baixo passam a mesclar entre si dentro do cap de 8 (RULE-01): 4 slots do mesmo grupo com o critério completo original (cor+tecido+estoque) e 4 slots do grupo cruzado com critério reduzido (cor+estoque, tecido NUNCA considerado) — cada bloco ranqueado independentemente pela MESMA cascata D-13 já existente (D-30, nenhum comparador novo). O gate de elegibilidade de nível superior da fonte é relaxado (D-34): fonte sem tecido canônico ainda gera o bloco cruzado, mas nunca o bloco mesmo-grupo.

O motor permanece zero-import/zero-I/O (RULE-02): as constantes/funções de grupo são DUPLICADAS aqui (nunca importadas de `product-group.js`, Plano 03.1-01) — o motor só consome `productGroupCanonical` já resolvido pela ingestão, nunca uma categoria crua nem uma tabela de mapeamento.

Purpose: este é o núcleo da emenda de RULE-01 desta fase — sem ele não há mescla real nem cota 4+4, apenas o comportamento antigo da Fase 3.
Output: `recommendation-engine.js` + `recommendation-engine.test.js` estendidos, todos os comportamentos (16 antigos + novos) verdes em vitest.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-criterio-de-grupo-de-produtos-no-motor-de-recomendacao-look-/03.1-CONTEXT.md
@.planning/phases/03.1-criterio-de-grupo-de-produtos-no-motor-de-recomendacao-look-/03.1-RESEARCH.md
@.planning/phases/03.1-criterio-de-grupo-de-produtos-no-motor-de-recomendacao-look-/03.1-PATTERNS.md
@.planning/phases/03-motor-de-recomenda-o-determin-stico/03-CONTEXT.md
@app-partners-recomendados/src/recommendation/recommendation-engine.js
@app-partners-recomendados/src/recommendation/recommendation-engine.test.js
</context>

<interfaces>
<!-- Contratos que o executor usa diretamente. -->

Shape de entrada estendido — `CatalogProductEntry` ganha um campo (produzido pela ingestão, Plano 03.1-04; nos testes deste plano, ajustado manualmente via fixture):

```javascript
{
  productId: string,
  name: string|null,
  colorValue: string|null,
  fabricTagCanonical: string|null,
  productGroupCanonical: string|null,  // NOVO (D-26): 'Look Inteiro' | 'Partes de Cima' | 'Partes de Baixo' | null
  hasAvailableGrade: boolean,
  variants: [ { variantId: string, sizeValue: string|null, stockTotal: number } ]
}
```

Shape de saída estendido — `Recommendation` (D-18 + productGroupCanonical, aditivo, nunca remove campo existente):

```javascript
{
  productId: string,
  colorValue: string,
  fabricTagCanonical: string|null,      // pode ser null no bloco cruzado (D-28)
  productGroupCanonical: string|null,   // NOVO
  stockTotal: number,
  sizesWithStock: number,
  centralSizesStock: number,
  stockBySize: [ { sizeValue: string|null, stock: number } ],
}
```

Assinatura pública inalterada (compatibilidade com a Fase 4, `04-CONTEXT.md` D-20): `recommendForProduct(productId, catalogProducts, { maxRecommendations = MAX_RECOMMENDATIONS } = {})`.

**Pseudocódigo de referência já escrito no `03.1-RESEARCH.md` (`## Architecture Patterns > Pattern 2`)** — usar como base, mas aplicar as duas correções documentadas abaixo (o RESEARCH.md já sinaliza a primeira como nota de robustez; a segunda é nova, encontrada nesta etapa de planejamento):

1. **Filtro de grupo explícito no caminho Look Inteiro** (já sinalizado no RESEARCH.md): `isEligibleCandidateInGroup` deve ser chamada também no caminho não-mesclável, com `targetGroup = sourceGroup`, em vez de reaproveitar a antiga `isEligibleCandidate` sem noção de grupo.
2. **Fail-closed para grupo-fonte null/desconhecido** (correção nova desta etapa de planejamento, não estava no RESEARCH.md): se `source.productGroupCanonical` for `null`, a função deve retornar `[]` IMEDIATAMENTE, antes de qualquer outra lógica. Sem essa guarda, dois produtos com `productGroupCanonical: null` (categoria não mapeada) que também coincidissem em cor+tecido+estoque se tornariam elegíveis um para o outro por `candidate.productGroupCanonical !== targetGroup` nunca ser verdadeiro quando ambos são `null` — exatamente o "fail-open incorreto" que o `03.1-RESEARCH.md` (Security Domain) explicitamente proíbe. Esta guarda é obrigatória.
</interfaces>

## Artifacts this phase produces (deste plano)

| Artifact | Type | Provides |
|----------|------|----------|
| `GROUP_LOOK_INTEIRO`, `GROUP_PARTES_DE_CIMA`, `GROUP_PARTES_DE_BAIXO`, `GROUP_QUOTA_PER_SIDE` | constantes exportadas (duplicadas de `product-group.js`, nunca importadas — RULE-02) | Nomes de grupo e cota fixa (D-28: 4) nomeados, nunca valor mágico |
| `crossGroupOf(group)` (interno ao motor, não exportado) | função duplicada | Par de mescla D-27/D-28 dentro do motor |
| `isEligibleCandidateInGroup(source, candidate, targetGroup, { requireFabric })` | função interna (substitui `isEligibleCandidate`) | Elegibilidade parametrizada por grupo-alvo e exigência de tecido |
| `buildSortedPool(source, catalog, targetGroup, { requireFabric })` | função interna nova | Monta e ordena (D-13) um pool de candidatos de um grupo-alvo específico |
| `composeGroupQuota(samePoolSorted, crossPoolSorted, { quota, cap })` | função interna nova | Cota 4+4 com backfill simétrico (D-29), ordem mesmo-grupo primeiro (D-35) |
| `buildRecommendation` (estendida) | função interna existente, modificada | Objeto rico agora inclui `productGroupCanonical` (D-18 estendido) |
| `recommendForProduct` (reescrita) | função exportada existente, corpo modificado | Orquestra Look Inteiro (auto-contido) vs. mescla Partes de Cima/Baixo (cota+backfill), mesma assinatura pública |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Look Inteiro auto-contido + caso base da mescla (sem backfill) + D-18 estendido (RED→GREEN)</name>
  <files>app-partners-recomendados/src/recommendation/recommendation-engine.js, app-partners-recomendados/src/recommendation/recommendation-engine.test.js</files>
  <read_first>
    .planning/phases/03.1-criterio-de-grupo-de-produtos-no-motor-de-recomendacao-look-/03.1-CONTEXT.md
    .planning/phases/03.1-criterio-de-grupo-de-produtos-no-motor-de-recomendacao-look-/03.1-RESEARCH.md
    .planning/phases/03-motor-de-recomenda-o-determin-stico/03-CONTEXT.md
    app-partners-recomendados/src/recommendation/recommendation-engine.js
    app-partners-recomendados/src/recommendation/recommendation-engine.test.js
  </read_first>
  <behavior>
    - Test 17: a factory `makeProduct` ganha um novo parâmetro `productGroupCanonical` com default `GROUP_LOOK_INTEIRO` (importada do próprio módulo do motor); os 16 testes existentes (Tests 1-16) continuam passando SEM modificação de asserção (só a factory muda), confirmando regressão zero para fixtures Look Inteiro
    - Test 18: fonte de Look Inteiro com um candidato de OUTRO grupo (`productGroupCanonical: GROUP_PARTES_DE_CIMA`) mas mesma cor + mesmo tecido + estoque disponível → candidato EXCLUÍDO (D-27, filtro de grupo explícito — robustez sobre o comportamento pré-03.1, que não olhava grupo nenhum)
    - Test 19: fonte com `productGroupCanonical: null` e um candidato TAMBÉM com `productGroupCanonical: null`, mesma cor + mesmo tecido + estoque disponível → retorna `[]` (fail-closed do grupo-fonte desconhecido — sem esta guarda, os dois nulls coincidiriam incorretamente)
    - Test 20: fonte de Partes de Cima com exatamente 4 elegíveis mesmo-grupo (cor+tecido+estoque, todos Partes de Cima) e 4 elegíveis do grupo cruzado (cor+estoque apenas, todos Partes de Baixo, tecido variado incluindo null) → retorna os 8, com os 4 do mesmo-grupo aparecendo ANTES dos 4 do cruzado no array (D-28 caso base + D-35 ordem básica)
    - Test 21: dentro do bloco cruzado, um candidato com `fabricTagCanonical: null` É elegível (tecido não considerado, D-28); dentro do bloco mesmo-grupo, um candidato com `fabricTagCanonical: null` NÃO é elegível (D-15 aplicado só a este bloco)
    - Test 22: fonte de Partes de Baixo SEM `fabricTagCanonical` (null) mas com candidatos elegíveis no grupo cruzado (Partes de Cima, cor+estoque) → retorna APENAS o bloco cruzado (o bloco mesmo-grupo fica vazio, mas a chamada não retorna `[]` inteiro — D-34, gate relaxado)
    - Test 23: cada `Recommendation` retornado tem exatamente os campos D-18 + `productGroupCanonical` (atualiza a asserção `Object.keys(...).sort()` do antigo Test 8 para incluir o novo campo)
  </behavior>
  <action>
    Em `recommendation-engine.js` (arquivo existente — preservar cabeçalho, JSDoc typedefs, `MAX_RECOMMENDATIONS`, `CENTRAL_SIZES_LETTER/NUMERIC`, os três comparadores D-13 e `compareRecommendations` intactos):

    1. Adicionar as constantes `GROUP_LOOK_INTEIRO = 'Look Inteiro'`, `GROUP_PARTES_DE_CIMA = 'Partes de Cima'`, `GROUP_PARTES_DE_BAIXO = 'Partes de Baixo'`, `GROUP_QUOTA_PER_SIDE = 4` (D-28, nomeada, nunca `4` mágico inline), todas exportadas. Comentário obrigatório no topo destas constantes explicando por que são DUPLICADAS de `product-group.js` em vez de importadas: RULE-02 exige zero imports no motor; o motor nunca resolve categoria crua, só consome `productGroupCanonical` já resolvido pela ingestão.

    2. Adicionar `crossGroupOf(group)` (função interna, NÃO exportada — mesmo comportamento de `product-group.js`: `GROUP_PARTES_DE_CIMA` → `GROUP_PARTES_DE_BAIXO`, `GROUP_PARTES_DE_BAIXO` → `GROUP_PARTES_DE_CIMA`, qualquer outro valor (incluindo `GROUP_LOOK_INTEIRO`, `null`) → `null`).

    3. Substituir a função `isEligibleCandidate(source, candidate)` por `isEligibleCandidateInGroup(source, candidate, targetGroup, { requireFabric })` — NÃO manter as duas (anti-padrão de duplicação de lógica, per `03.1-RESEARCH.md` "Anti-Patterns to Avoid"). Regras, na ordem: `candidate` nulo → false; auto-exclusão (`String(candidate.productId) === String(source.productId)`) → false; `!candidate.hasAvailableGrade` → false; `candidate.colorValue == null` → false; `candidate.productGroupCanonical !== targetGroup` → false (isto sozinho já barra candidatos de grupo `null`/diferente, incluindo Look Inteiro contra outro grupo — Test 18); se `requireFabric` truthy: `candidate.fabricTagCanonical == null` → false, e cor+tecido precisam bater via `normalizeMatchValue` (mesma normalização já existente); se `requireFabric` falsy: tecido NUNCA é considerado (nem filtro nem exigência de não-nulo, D-28) — só cor precisa bater. Retornar o resultado da comparação de cor no final (`normalizeMatchValue(candidate.colorValue) === normalizeMatchValue(source.colorValue)`).

    4. Adicionar `buildSortedPool(source, catalog, targetGroup, { requireFabric })` (interna, não exportada): filtra `catalog` por `isEligibleCandidateInGroup`, mapeia com `buildRecommendation`, ordena com `compareRecommendations` (D-13, cascata inalterada) — retorna o array já ordenado, sem cortar ainda.

    5. Adicionar `composeGroupQuota(samePoolSorted, crossPoolSorted, { quota = GROUP_QUOTA_PER_SIDE, cap = MAX_RECOMMENDATIONS } = {})` (interna, não exportada) — NESTA TASK, implementar APENAS o caso base SEM backfill: `[...samePoolSorted.slice(0, quota), ...crossPoolSorted.slice(0, quota)].slice(0, cap)`. O backfill (D-29) é adicionado na Task 2 deste mesmo plano — não implementar ainda, mas deixar a assinatura da função já pronta para receber a extensão sem quebrar a Task 1.

    6. Estender `buildRecommendation(candidate)`: adicionar `productGroupCanonical: candidate.productGroupCanonical ?? null` ao objeto retornado (campo aditivo, nenhum campo existente removido ou renomeado).

    7. Reescrever o corpo de `recommendForProduct` (assinatura pública inalterada): após `if (!source) return [];`, trocar a checagem antiga (`fabricTagCanonical == null || colorValue == null`) por: `if (source.colorValue == null) return [];` seguido de `const sourceGroup = source.productGroupCanonical; if (sourceGroup == null) return [];` (guarda fail-closed obrigatória, ver `<interfaces>` correção 2). Depois `const crossGroup = crossGroupOf(sourceGroup);`. Se `!crossGroup` (Look Inteiro ou qualquer grupo não-mesclável): `if (source.fabricTagCanonical == null) return [];` (tecido continua sempre obrigatório para este caminho, D-27/D-15 inalterados) e então `return buildSortedPool(source, catalog, sourceGroup, { requireFabric: true }).slice(0, maxRecommendations);`. Se `crossGroup` existir (Partes de Cima/Baixo): `const samePool = source.fabricTagCanonical != null ? buildSortedPool(source, catalog, sourceGroup, { requireFabric: true }) : [];` (D-34: gate de tecido relaxado, aplicado só a este bloco) e `const crossPool = buildSortedPool(source, catalog, crossGroup, { requireFabric: false });` (nunca exige tecido), retornando `composeGroupQuota(samePool, crossPool, { quota: GROUP_QUOTA_PER_SIDE, cap: maxRecommendations })`.

    8. Atualizar o JSDoc typedef `CatalogProductEntry` (adicionar `@property {string|null} productGroupCanonical`) e `Recommendation` (idem) no topo do arquivo. Atualizar o comentário de cabeçalho do módulo citando D-26 a D-31, D-34, D-35.

    Em `recommendation-engine.test.js`: estender o `import` para trazer `GROUP_LOOK_INTEIRO, GROUP_PARTES_DE_CIMA, GROUP_PARTES_DE_BAIXO` além de `recommendForProduct, MAX_RECOMMENDATIONS` já existentes. Estender a factory `makeProduct({ productId, colorValue, fabricTagCanonical, hasAvailableGrade, variants, productGroupCanonical = GROUP_LOOK_INTEIRO } = {})` — default `GROUP_LOOK_INTEIRO` garante que os 16 testes existentes (que nunca definem grupo) continuem comparando produtos do MESMO grupo entre si, preservando comportamento idêntico. Atualizar a asserção de `Object.keys` do Test 8 existente para incluir `'productGroupCanonical'` na lista esperada (ordenada). Adicionar um novo `describe('recommendForProduct - grupo e mescla (D-27-D-30, D-34, D-35)')` com os Tests 17-23. Escrever os testes novos PRIMEIRO (RED), então implementar até verde (GREEN) — rodando a suíte completa do arquivo a cada passo para confirmar que os Tests 1-16 permanecem verdes.
  </action>
  <acceptance_criteria>
    - `cd app-partners-recomendados && npx vitest run src/recommendation/recommendation-engine.test.js` sai com código 0, cobrindo os Tests 1-23 (16 originais + 7 novos)
    - `src/recommendation/recommendation-engine.js` contém `function composeGroupQuota`, `function buildSortedPool`, `function isEligibleCandidateInGroup`, `export const GROUP_QUOTA_PER_SIDE`
    - `cd app-partners-recomendados && grep -c "function isEligibleCandidate(" src/recommendation/recommendation-engine.js` retorna `0` (a função antiga foi substituída, não duplicada ao lado da nova)
    - `cd app-partners-recomendados && ! grep -v '^\s*//' src/recommendation/recommendation-engine.js | grep -Eq '^import '` sai com código 0 (motor continua com zero linhas de importação após a extensão — RULE-02)
    - Teste do Test 19 (fail-closed de grupo-fonte null) passa mesmo com um candidato "irmão" também `productGroupCanonical: null` e cor+tecido+estoque idênticos — prova a guarda, não apenas o caminho feliz
  </acceptance_criteria>
  <verify>
    <automated>cd app-partners-recomendados && npx vitest run src/recommendation/recommendation-engine.test.js</automated>
  </verify>
  <done>Look Inteiro permanece auto-contido com filtro de grupo explícito (D-27); Partes de Cima/Baixo compõem cota base 4+4 sem backfill ainda (D-28); fonte com grupo desconhecido/null falha fechado; fonte sem tecido gera bloco cruzado (D-34); Recommendation inclui productGroupCanonical (D-18 estendido); 23 comportamentos verdes, motor ainda zero-import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backfill simétrico (D-29) + confirmação de cascata/ordem (D-30/D-35) + regressão completa (RED→GREEN)</name>
  <files>app-partners-recomendados/src/recommendation/recommendation-engine.js, app-partners-recomendados/src/recommendation/recommendation-engine.test.js</files>
  <read_first>
    app-partners-recomendados/src/recommendation/recommendation-engine.js
    app-partners-recomendados/src/recommendation/recommendation-engine.test.js
    .planning/phases/03.1-criterio-de-grupo-de-produtos-no-motor-de-recomendacao-look-/03.1-CONTEXT.md
  </read_first>
  <behavior>
    - Test 24: bloco mesmo-grupo com apenas 2 elegíveis + bloco cruzado com >= 6 elegíveis → retorna os 2 do mesmo-grupo + 6 do cruzado (o cruzado preenche os 2 slots vazios da cota do mesmo-grupo usando SEU PRÓPRIO restante, após seus 4 da cota) = 8 total (D-29)
    - Test 25: cenário espelhado — bloco cruzado com apenas 1 elegível + bloco mesmo-grupo com >= 7 elegíveis → retorna 4 do mesmo-grupo (cota) + 1 do cruzado + 3 de backfill do mesmo-grupo (usando seu restante após a cota) = 8 total
    - Test 26: os dois blocos somados têm menos que 8 elegíveis no total (ex: 2 mesmo-grupo + 3 cruzado) → retorna exatamente os 5 existentes, nunca inventando candidato inelegível para completar 8
    - Test 27: a cascata D-13 decide a ordem DENTRO de cada bloco de forma independente — fixture com `stockTotal` variado tanto no pool mesmo-grupo quanto no pool cruzado confirma que AMBOS os blocos aparecem ordenados por estoque decrescente (mesmo comparador, sem nível novo por grupo)
    - Test 28: ordem final do array quando backfill está em jogo — bloco mesmo-grupo (+ seu backfill, se houver) aparece INTEGRALMENTE antes do bloco cruzado (+ seu backfill, se houver); asserção explícita da sequência de `productId` (D-35)
    - Test 29: suíte completa do arquivo (`npx vitest run src/recommendation/recommendation-engine.test.js`) permanece 100% verde — os 16 testes originais da Fase 3 + os 12 novos desta fase (Tests 17-28)
  </behavior>
  <action>
    Estender `composeGroupQuota` (implementada na Task 1 sem backfill) para adicionar a lógica de backfill simétrico (D-29): calcular `const sameShortfall = quota - same.length;` e `const crossShortfall = quota - cross.length;` (onde `same`/`cross` são os slices de até `quota` já calculados); `const crossBackfill = crossShortfall > 0 ? samePoolSorted.slice(quota, quota + crossShortfall) : [];` (o lado cruzado, quando insuficiente, é preenchido pelo RESTANTE do pool mesmo-grupo — já ordenado, continuando exatamente de onde sua própria cota parou, respeitando seu critério de `requireFabric: true`); simetricamente `const sameBackfill = sameShortfall > 0 ? crossPoolSorted.slice(quota, quota + sameShortfall) : [];`. Retornar `[...same, ...sameBackfill, ...cross, ...crossBackfill].slice(0, cap)` — ORDEM OBRIGATÓRIA (D-35): bloco mesmo-grupo completo (cota + seu backfill) antes do bloco cruzado completo (cota + seu backfill). Comentário citando D-29 (nunca inventa candidato — se a soma de `samePoolSorted.length + crossPoolSorted.length` for menor que `cap`, o resultado final tem menos que `cap` itens, o que é esperado e correto) e D-35 (ordem do array).

    Nenhuma mudança adicional é esperada em `isEligibleCandidateInGroup`, `buildSortedPool` ou no corpo de `recommendForProduct` além do já escrito na Task 1 — esta task é focada em completar `composeGroupQuota` e comprovar, com fixtures novos, que o restante do pipeline (cascata D-13 por bloco, ordem final, fail-closed, gate relaxado) já escrito na Task 1 se comporta corretamente sob condições de escassez de candidatos.

    Escrever os Tests 24-28 PRIMEIRO (RED — devem falhar contra a `composeGroupQuota` sem backfill da Task 1, comprovando que o backfill realmente não existia ainda), implementar o backfill (GREEN), e por fim rodar a suíte completa do arquivo (Test 29) confirmando que absolutamente nada regrediu.
  </action>
  <acceptance_criteria>
    - `cd app-partners-recomendados && npx vitest run src/recommendation/recommendation-engine.test.js` sai com código 0, cobrindo os Tests 1-29 (28 comportamentos específicos + a checagem de suíte completa)
    - Teste do Test 24 e Test 25 comprovam backfill em AMBAS as direções (mesmo-grupo escasso preenchido pelo cruzado, e vice-versa)
    - Teste do Test 26 comprova que o motor nunca excede o total real de elegíveis somados dos dois blocos, mesmo quando esse total é menor que 8
    - `cd app-partners-recomendados && npx vitest run` (suíte completa do projeto) sai com código 0 — nenhuma regressão em `fabric-taxonomy.test.js`, `stock-availability.test.js`, `adaptive-limiter.test.js` ou `product-group.test.js` (Plano 03.1-01, já mesclado nesta wave)
    - `cd app-partners-recomendados && ! grep -v '^\s*//' src/recommendation/recommendation-engine.js | grep -Eq '^import '` continua saindo com código 0 (motor permanece sem nenhuma importação após a Task 2)
  </acceptance_criteria>
  <verify>
    <automated>cd app-partners-recomendados && npx vitest run</automated>
  </verify>
  <done>Cota 4+4 completa com backfill simétrico em ambas as direções (D-29), nunca inventando candidato inelegível; cascata D-13 confirmada independente por bloco (D-30); ordem final mesmo-grupo-antes-de-cruzado confirmada mesmo com backfill (D-35); suíte completa do projeto verde, motor sem nenhuma importação.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `productGroupCanonical` já resolvido pela ingestão (Plano 03.1-04) → decisão de pool/cota do motor | O motor confia que o valor já foi resolvido corretamente; não pode tratar `null`/valor desconhecido como elegível por padrão |
| Saída do motor (com bloco cruzado sem tecido) → aprovação humana (Fase 4) e escrita na loja (Fase 5) | O resultado precisa continuar determinístico e auditável mesmo quando a composição envolve dois blocos com critérios diferentes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-02 | Tampering (grupo-fonte ou candidato com grupo `null`/desconhecido tratado como elegível por padrão) | `recommendation-engine.js` (`recommendForProduct`, `isEligibleCandidateInGroup`) | mitigate | Guarda explícita `if (sourceGroup == null) return [];` antes de qualquer composição de pool (Test 19); candidatos são filtrados por `candidate.productGroupCanonical !== targetGroup`, que exclui `null`/grupo diferente por construção, nunca por coincidência de dado |
| T-03.1-03 | Repudiation (composição de grupo quebra determinismo/auditabilidade do motor) | `recommendation-engine.js` | mitigate | `composeGroupQuota`/`buildSortedPool` são funções puras sem relógio/aleatoriedade; a MESMA cascata `compareRecommendations` (D-13) ordena cada bloco; regressão completa (Test 29) confirma que a suíte de determinismo/pureza da Fase 3 (Tests 9, 10, 15, 16) permanece intacta |
| T-03.1-SC | Tampering (supply chain em instalação de pacote) | npm | accept | Nenhum pacote novo é instalado neste plano |
</threat_model>

<verification>
- `cd app-partners-recomendados && npx vitest run src/recommendation/recommendation-engine.test.js` — 29 comportamentos verdes (16 regressão Fase 3 + 13 novos desta fase)
- `cd app-partners-recomendados && npx vitest run` — suíte completa do projeto verde
- `cd app-partners-recomendados && ! grep -v '^\s*//' src/recommendation/recommendation-engine.js | grep -Eq '^import '` — motor sem nenhuma importação de módulo
</verification>

<success_criteria>
- D-26 a D-31, D-33 (implicações), D-34, D-35 implementados exatamente como travados/pesquisados
- D-13/D-14/D-15/D-17/D-18 (Fase 3) preservados sem regressão para fixtures Look Inteiro
- Motor permanece função pura, zero-import, zero-I/O (RULE-02) mesmo após a extensão
- Assinatura pública de `recommendForProduct` inalterada (compatibilidade com a Fase 4, D-20 do `04-CONTEXT.md`)
</success_criteria>

<output>
Create `.planning/phases/03.1-criterio-de-grupo-de-produtos-no-motor-de-recomendacao-look-/03.1-02-SUMMARY.md` when done
</output>
