# Bot de Produtos Recomendados — Talgui

## What This Is

Uma ferramenta interna que automatiza a curadoria da vitrine de "Recomendados" de cada produto na loja Talgui (moda feminina, Nuvemshop plano Next, 592 produtos). Hoje esse trabalho é 100% manual e se desfaz sozinho conforme produtos esgotam. A solução é um motor determinístico (sem IA/ML) que mantém recomendações compatíveis (mesma cor, mesmo tipo de tecido, com estoque disponível) atualizadas automaticamente, com aprovação humana antes de qualquer alteração na loja, rodando na nuvem com retroalimentação diária.

Este projeto já foi iniciado em outra máquina (ver PDF "Resumo Executivo Talgui Recomendados 2026-06-24" trazido pelo usuário) e está sendo retomado aqui. O contexto desse resumo foi incorporado integralmente, com uma correção crítica de escopo descrita abaixo.

## Core Value

A vitrine de "Recomendados" se mantém sempre curada e sem estoque zerado, sem trabalho manual — e sem depender de uma escolha de plataforma (App Sob Medida vs Partners) que se prove tecnicamente inviável a meio do caminho.

## Requirements

### Validated

(Nenhuma ainda — nada foi confirmado como funcional nesta retomada do projeto. Ver nuance crítica abaixo.)

### Validated

- ✓ Nuvemshop expõe API pública de app (App Sob Medida e Partners) para leitura de catálogo/estoque/variantes via `GET /2025-03/{store_id}/products` — testado ao vivo na loja real Talgui, token funcional, `200 OK` com todos os campos documentados (`name`, `categories`, `variants`, `tags`, `has_stock`, etc.) — validação técnica desta retomada, 2026-07-08
- ✓ O campo nativo "Produtos Relacionados" (`alternative_products`/`complementary_products`) **não é gravável nem legível via nenhuma API pública de app** (nem App Sob Medida, nem, por extensão de arquitetura, Partners) — é gravado exclusivamente pelo endpoint interno `cirrus.tiendanube.com/v4/products/{id}`, autenticado por sessão de navegador logado no admin (`X-Access-Token`), não por `Authorization: Bearer` de app. Confirmado por: (1) inspeção de rede do admin capturando o payload real do campo, (2) teste ao vivo mostrando que a API pública não retorna esses campos ao ler o mesmo produto, (3) teste ao vivo mostrando que o endpoint interno rejeita o token de app (404, roteamento diferente do `200` obtido com sessão de navegador). Isso invalida a afirmação do PDF original de que a viabilidade "já foi confirmada" — o teste anterior provavelmente validou outro campo (categoria/tag/ordenação), não o campo nativo de recomendados.
- ✓ Nuvemshop Partners (app privado, "Exclusivo para Lojistas Selecionados", não-homologado) consegue escrever no storefront real da Talgui via `write_scripts` — validado empiricamente porque já está em produção no projeto paralelo de ordenação automática de vitrines do usuário, na mesma loja
- ✓ **Validado em Fase 1 (2026-07-10, spike de viabilidade end-to-end, 1 produto real):** a arquitetura completa funciona de ponta a ponta — App Partners privado novo ("Talgui Recomendados") autentica via OAuth contra a loja real (PLAT-01); Metafield gravado via API pública e confirmado por leitura de volta (WRTE-01); endpoint próprio somente-leitura expõe a recomendação sem nunca vazar o token OAuth (PLAT-05); bloco "Recomendados" customizado renderiza visivelmente na página real do produto, na posição exata onde o bloco nativo aparecia, sem conflito visual (FRNT-01, PLAT-04). Decisão formal registrada em `01-05-DECISAO.md`: arquitetura confirmada viável, roadmap prossegue.
- ✓ **Tema ativo da loja Talgui é "Morelia", não "Patagonia"** (PLAT-03) — NubeSDK não é suportado nativamente; formulário de ativação ainda não submetido. **Decisão D-11 (2026-07-10):** enquanto a ativação não é aprovada, o Script de storefront roda via Script API tradicional (legada, `write_scripts` sem NubeSDK) como v.Alpha assumido — funcional e validado em produção, mas com prazo de vida garantido de poucas semanas (bloqueio de novas instalações a partir de 30/ago/2026, remoção progressiva a partir de 30/out/2026) e reconstrução total necessária quando o NubeSDK for aprovado. Isso não bloqueia o avanço do projeto — usuário decidiu explicitamente avançar em paralelo (motor/catálogo/UI), não esperar a aprovação do NubeSDK.
- ✓ **Validado em Fase 2 (2026-07-11, ingestão de catálogo e qualidade de dados, categoria Vestidos completa):** leitura paginada do catálogo real via API pública com rate limiter adaptativo (lê headers `x-rate-limit-*` reais, nunca hardcoded) — PLAT-02; disponibilidade de estoque calculada via `inventory_levels[]` (grade ≥ 3 tamanhos) — DATA-01; baseline de recomendações atuais lida via Metafields antes de qualquer nova computação — DATA-02; auditoria de tags de tecido roda a cada execução (nunca fuzzy-match, tags não mapeáveis sinalizadas explicitamente) — DATA-03. Tudo persistido em SQLite (schema + transação única por execução). Comprovado ao vivo 4 vezes contra a categoria real "Vestidos" (execução final pós-fix: `run_id=4`, 645 produtos, 143 disponíveis, sem erros). **Contagem real da categoria Vestidos é 645 produtos, não os ~628 estimados em D-01/D-02** (achado da Fase 2, atualiza a estimativa do PDF original). Nenhuma tag de tecido estava mapeada para valor canônico neste momento — planilha de padronização (D-07) ainda não tinha sido importada, o que era esperado nesta etapa. **Atualização 2026-07-15:** D-07 foi concluído pelo usuário (tags de tecido aplicadas em massa no catálogo real). A resolução de canônico mudou de match exato para contenção de palavra-chave (D-32, ver `02-CONTEXT.md`) — validado ao vivo: 354/675 produtos reais (categoria Vestidos, run_id 5) agora têm tecido canônico preenchido, e o motor já gera recomendações reais (ver `recommend-cli.js`).
- ✓ **Decisão A4 (Fase 2, 2026-07-10):** tags de tecido serão armazenadas no campo nativo `product.tags` (não em Metafield customizado dedicado) — usuário decidiu misturar o valor de tecido com as demais tags de marketing/SEO já existentes nesse campo, ao invés de criar um namespace/key separado.
- ✓ **Validado em Fase 03.1 (2026-07-16, critério de Grupo de Produtos):** o motor (`recommendation-engine.js`) ganhou um quarto critério de elegibilidade — Grupo de Produtos (Look Inteiro / Partes de Cima / Partes de Baixo, D-26 a D-35) — com cota fixa 4+4 e backfill simétrico entre Partes de Cima/Baixo, preservando 100% dos 16 testes já verdes de Look Inteiro e mantendo o motor zero-import/zero-I/O (RULE-02, grupo resolvido já na ingestão via novo `product-group.js`). Ingestão (`ingest-catalog.js`) agora suporta múltiplas categorias no mesmo run (D-33), e o schema persiste `category_raw`/`product_group_canonical` por produto. Todas as 7 success criteria da fase confirmadas pelo verificador direto no código (não apenas nos SUMMARYs); suíte completa 64/64 testes verdes, sem regressão. Nenhum produto real ainda tem `product_group_canonical` preenchido — rodar a ingestão contra o catálogo real é um passo operacional do usuário, não um entregável de código desta fase.
- ✓ **Validado em Fase 05 (2026-07-16/17, gravação segura em produção):** escrita real em Metafield contra a loja Talgui confirmada de ponta a ponta contra produção (não apenas mocks) — `write-executor.js` grava snapshot (`previous_value`) e valor novo (`written_value`) na tabela `write_log` (D-41, combina snapshot + log de auditoria numa única linha append-only) antes/depois de toda escrita real (WRTE-02). Rollback (`scripts/rollback.js`, D-37) confirmado restaurando o valor exato via leitura ao vivo + comparação estrita contra divergência (D-38) antes de qualquer efeito — aborta com `RollbackConflictError` em vez de sobrescrever silenciosamente (WRTE-03). Log de auditoria (`GET /audit`) confirmado visualmente mostrando escritas manuais e rollbacks em ordem cronológica (WRTE-04). Notificação de falha via webhook (`notify-failure.js`) implementada e testada, nunca vazando credenciais (WRTE-05). Verificação humana real executada contra o produto de teste 349886153 (mesmo da Fase 1): round-trip de escrita, rollback e auditoria visual todos confirmados na loja de produção real — ver `05-UAT.md`. Auditoria de segurança (15/15 ameaças STRIDE) fechada — ver `05-SECURITY.md`.

### Active

- [ ] Motor de recomendação determinístico: até 8 recomendados por produto, mesma cor + mesmo tipo de tecido + estoque disponível (obrigatório) + grupo de produtos (Fase 03.1), priorizando maior giro entre elegíveis — lógica de seleção/matching implementada e testada (Fases 1-3, 03.1); prioridade por "maior giro" (sales velocity) ainda não confirmada como implementada — cascata de desempate atual é por estoque (D-13), não por giro
- [ ] Bloco "Recomendados" em formato carrossel, exibindo até 8 produtos (limite nativo da Nuvemshop para esse tipo de ordenação) — Fase 1 validou apenas 1 produto/link simples, layout carrossel ainda não construído
- [ ] Retroalimentação do motor quando um produto recomendado entra em stockout (grade reduzida ou todos os tamanhos indisponíveis) — comportamento ainda não testado
- [ ] Painel web de aprovação (substitui a planilha do plano original) — preview "antes vs. depois" revisável antes de qualquer escrita na loja, incluindo mecanismo de intervenção manual humana na curadoria dos "Recomendados"
- [ ] Execução na nuvem (ex: GitHub Actions ou servidor/cron hospedado) — não depende da máquina pessoal estar ligada
- [ ] Snapshot diário automático: retroalimentação recorrente que recalcula recomendações com base em critérios atualizados de estoque e disponibilidade de cor/tecido
- [ ] Reconstrução do Script de storefront em NubeSDK (quando a ativação for aprovada para o tema Morelia) — substitui o v.Alpha da Script API tradicional construído na Fase 1
- [x] **Painel administrativo de leitura/monitoramento do Recom** (distinto do painel de aprovação da linha acima — este é somente-leitura, sem intervenção humana na curadoria): contagem de produtos relacionados, estoque dos relacionados, cobertura de tag de tecido, status/horário do último cron. Construído fora do roadmap GSD (pedido direto do usuário, 2026-08-09), **validado em produção**: `https://recom-admin-talgui.vercel.app` (projeto Vercel separado do endpoint público, protegido por Basic Auth). **Refinamento concluído (2026-08-10):** usuário usou o painel real e pediu ajustes nos 4 cards — todos implementados e verificados em produção na mesma sessão: Card 2 mostra "Recomendados sem estoque"; Card 3 (tecido) e Card 4 (cron) ganharam expansão com detalhamento e export Excel (`?format=xlsx`, gerado com `exceljs`). Ver `.planning/.continue-here.md` para detalhes.
- [x] **Talgui App Center**: hub externo (repo `danilo-t4lgu1/talgui-app-center`, fora deste repositório) que lista todos os apps internos da Talgui como cards clicáveis — Recom é o primeiro card funcional. **Validado em produção** (2026-08-09, retomada): `https://talgui-app-center.vercel.app`, Basic Auth confirmada (401/401/200). Ver D-74 — o "status indefinido na CLI" era na verdade um deploy bloqueado por e-mail de commit inválido, não um problema de código/deploy.

### Out of Scope

- IA/ML para geração de recomendações — motor é 100% baseado em regras determinísticas, auditável e barato
- Edição de tema/Script da loja via App Sob Medida — se necessário, projeto migra para Nuvemshop Partners
- ~~Escrita automática sem aprovação humana — toda gravação na loja passa por aprovação prévia~~ **REVERTIDO na Fase 7 (D-61, 2026-07-21):** para o **caminho automático/agendado**, o motor determinístico grava sem aprovação prévia. A redação original fica riscada para rastreabilidade. O **caminho manual** (escrita disparada explicitamente por humano) permanece inalterado; o que muda é que o regime diário deixa de exigir portão prévio

## Context

**Histórico:** projeto retomado de outra máquina local. Um resumo executivo (PDF, 2026-06-24) documentava 9 etapas, com a Etapa 1 ("Viabilidade") marcada como "Concluída" — alegando que já foi provado que dá para escrever recomendações pela API e a loja exibe corretamente, sem mexer no tema.

**Nuance crítica (motivo da correção de escopo):** o usuário não tem certeza de ter validado essa viabilidade como de fato funcional em loja real. Além disso, em outro projeto anterior, o usuário descobriu ao final que a API de "App Sob Medida" (custom app) da Nuvemshop **não permite edição de Scripts** — não se comunica com a escrita nativa de Scripts da API da Nuvemshop. Isso não invalida necessariamente este projeto, porque o escopo real aqui é ler/escrever **dados de produto** (recomendações), não editar tema ou Script. Mas como o usuário não tem certeza de que a validação anterior foi de fato confirmada como funcional, a Etapa 1 do roadmap original foi rebaixada de "Concluída" para "Pendente de revalidação" e vira a primeira etapa real deste projeto.

**Resultado da revalidação (2026-07-08):** confirmado que o caminho App Sob Medida sozinho **não é viável** para este projeto. O campo nativo "Produtos Relacionados" só é gravável pelo endpoint interno do admin da Nuvemshop (sessão de navegador), inacessível a qualquer app via API pública. A arquitetura definida é:
1. Motor de recomendação roda na nuvem, lê catálogo/estoque via API pública (App Sob Medida ou Partners, tanto faz para essa parte) e grava o resultado em Metafields do produto (API pública, sem Script).
2. Um App Partners privado (não homologado, "Exclusivo para Lojistas Selecionados") com escopo `write_scripts` + NubeSDK injeta um Script no storefront que lê os Metafields e renderiza um bloco "Recomendados" customizado.
3. Esse mecanismo (Script via Partners escrevendo no storefront real da Talgui) já está validado em produção pelo projeto paralelo de ordenação automática de vitrines do usuário, na mesma loja — reduz o risco de novidade tecnológica.

Evidência técnica da investigação (não-destrutiva, sem alterar dados reais):
- Captura de rede do admin (DevTools) mostrando que a gravação de "Produtos relacionados" chama `PATCH https://cirrus.tiendanube.com/v4/products/{id}?sync-refresh=true` com payload `{alternative_products: [{id, order}], complementary_products: [...]}`, autenticado via header `X-Access-Token` de sessão de navegador (não `Authorization: Bearer` de app).
- `GET /2025-03/{store_id}/products/{id}` via API pública de app (token válido) no mesmo produto retornou `200 OK` mas sem os campos `alternative_products`/`complementary_products`/`related_products` — confirmando que não existem no contrato de API pública.
- `GET https://cirrus.tiendanube.com/v4/products/{id}` usando `Authorization: Bearer` de app retornou `404` (o endpoint interno não reconhece autenticação de app).

**Visão de futuro (v2 do PDF) incorporada como requisito ativo, não mais como "fora de escopo":**
- Agendador que roda sozinho periodicamente para recompor a vitrine automaticamente = snapshot diário retroalimentado (estoque + cor/tecido)
- Painel web de aprovação como alternativa à planilha → já definido como abordagem do MVP nesta retomada (não planilha)

**Execução:** roda na nuvem (GitHub Actions ou servidor/cron hospedado), não depende de máquina pessoal ligada — necessário para sustentar o snapshot diário.

## Constraints

- **Plataforma**: Nuvemshop plano Next, loja real "Talgui" (592 produtos) — qualquer escrita precisa respeitar rate limits da API (2 req/s, buffer de 40)
- **Modelo de app**: Nuvemshop Partners, app privado não homologado ("Exclusivo para Lojistas Selecionados"), com escopo `write_scripts` + NubeSDK — confirmado como obrigatório em 2026-07-08, pois o campo nativo de recomendados não é acessível via API pública de nenhum app
- **Sem IA/ML**: motor de recomendação deve ser determinístico e auditável
- **Segurança**: ~~nenhuma escrita na loja sem aprovação humana prévia~~ — **constraint revertida para o caminho automático na Fase 7 (Opção B, D-61, decisão explícita do usuário 2026-07-21)**. O motor determinístico grava **automaticamente** no regime diário, sem portão de aprovação prévia. A rede de segurança é mantida por outros meios: snapshot/rollback/auditoria (`write_log`, D-41; rollback em lote D-65) e guardas de execução — kill switch (D-62), disjuntor/circuit breaker (D-63) e Defesas 1/2 (integridade do snapshot D-66 e validação referencial na escrita D-67). Toda escrita continua capturando o estado anterior e permitindo rollback. O caminho manual permanece inalterado

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Rebaixar Etapa 1 ("Viabilidade") de Concluída para Pendente de revalidação | Usuário não tinha certeza de ter confirmado a escrita via API em loja real como funcional | ✓ Good — revalidação concluída, confirmou que a premissa do PDF estava errada |
| Migrar para Nuvemshop Partners privado (write_scripts) como caminho obrigatório | Confirmado empiricamente em 2026-07-08 que o campo nativo "Produtos Relacionados" só é gravável pelo endpoint interno do admin (sessão de navegador), inacessível a App Sob Medida ou qualquer API pública de app | ✓ Good |
| Arquitetura: motor grava em Metafields (API pública) + Script (Partners) renderiza bloco customizado no storefront | Contorna a limitação do campo nativo sem depender do endpoint interno da Nuvemshop; reaproveita mecanismo já validado em produção pelo projeto paralelo de ordenação | ✓ Good |
| Painel web de aprovação no MVP em vez de planilha | Usuário optou por adiantar a v2 do PDF (painel web) já no MVP desta retomada | ✓ Good |
| Execução na nuvem (não máquina pessoal) | Necessário para sustentar snapshot diário automático sem depender da máquina estar ligada | ✓ Good |
| Snapshot diário tratado como requisito ativo, não v2 fora de escopo | Usuário quer retroalimentação automática via estoque/cor/tecido incluída neste projeto | ✓ Good |
| D-11: construir o Script de storefront via Script API tradicional (v.Alpha), não NubeSDK, enquanto a ativação NubeSDK segue pendente | Não há prazo conhecido para a resposta da Nuvemshop; usuário preferiu validar/refinar a arquitetura de dados e UI agora, aceitando que o Script precisará ser reconstruído do zero em NubeSDK depois | ✓ Good — Fase 1 validada de ponta a ponta via este caminho |
| Avançar Fase 2+ (catálogo completo, carrossel de 8 produtos, motor real, stockout, curadoria manual) em paralelo à espera da ativação NubeSDK, não bloqueado por ela | Usuário: "não posso deixar uma versão minimamente aceitável esperando o NubeSDK" — quer usar o tempo de espera para já validar comportamentos reais do motor/UI | ✓ Good |
| A4: tags de tecido vivem no campo nativo `product.tags`, não em Metafield customizado dedicado | Nenhum Metafield de tecido existe hoje em nenhum produto real inspecionado; usuário optou por reaproveitar o campo já existente ao invés de criar um namespace separado | ✓ Good — confirmado ao vivo que `fabric-taxonomy.js` lê corretamente o campo nativo |
| D-41: `write_log` é uma única tabela append-only que serve simultaneamente de snapshot (WRTE-02) e log de auditoria (WRTE-04) | Evita duas tabelas/fontes de verdade separadas para o mesmo evento de escrita | ✓ Good — confirmado em produção: round-trip real (previous_value/written_value) e rollback (nova linha, `triggered_by='rollback'`) ambos corretos |
| D-38: rollback só restaura se o valor ao vivo bater exatamente com o `written_value` da última escrita; divergência aborta (`RollbackConflictError`) sem escrever | Uma edição manual no admin ou outra execução escrevendo por cima não deve ser silenciosamente sobrescrita por um rollback | ✓ Good — confirmado em produção, valor restaurado exatamente ao original |
| D-37: rollback só via CLI manual (`node scripts/rollback.js`), sem rota HTTP nova | Nenhuma necessidade operacional de expor rollback via painel web nesta fase; reduz superfície de ataque (T-05-12, risco aceito por postura de confiança local já estabelecida no projeto) | ✓ Good |
| D-61 (Fase 7, 2026-07-21): **Opção B — escrita automática, sem portão prévio.** O motor grava automaticamente no regime diário; **reverte** a constraint permanente "nenhuma escrita sem aprovação humana prévia", **aposenta o portão prévio do APRV-03** (para o caminho automático) e **altera o D-47** (job diário da Fase 6, que só populava a fila de aprovação, passa a calcular **e gravar**). O painel/`GET /audit` passa a ser verificação/auditoria **pós-escrita** | Decisão explícita e deliberada do usuário: sendo determinístico, o motor se retroalimenta e é assertivo dentro dos critérios (cor+tecido+estoque+grupo+visibilidade), então verificação prévia constante deixa de ser necessária. Segurança mantida por snapshot/rollback/auditoria + kill switch (D-62), disjuntor (D-63) e Defesas D-66/D-67 | ✓ Adotado na Fase 7 |
| D-70 (Fase 7, princípio-guia): refinar o **backend** (dados + motor + escrita) para reaproveitamento **total** na futura migração NubeSDK — nada nesta fase acopla lógica de negócio à apresentação | A arquitetura já é desacoplada (motor → Metafield → script); quando o NubeSDK for aprovado, só a camada de apresentação (Script v.Alpha, D-11) é reconstruída e todo o miolo funcional migra intacto | ✓ Good |
| D-71 (2026-08-09, fora do roadmap GSD): painel administrativo do Recom deployado em **projeto Vercel separado** (`recom-admin`), não no projeto público `app-partners-recomendados` | O endpoint `/api/recommendations/:id` é consumido ao vivo, sem login, pelo navegador de clientes anônimos no storefront — proteger esse projeto com login quebraria o carrossel em produção. Confirmado por teste real durante a sessão: endpoint público seguiu 200 OK o tempo todo | ✓ Good — verificado em produção |
| D-72 (2026-08-09): proteção do painel via **Basic Auth em Edge Middleware próprio**, não o toggle nativo "Vercel Authentication: All Deployments" | Esse toggle só cobre todos os deployments (incluindo os aliases `.vercel.app` de uso real) no plano **Pro** — no Hobby (plano atual) ele deixava os aliases públicos mesmo "ligado". Middleware próprio funciona igual no Hobby, sem custo | ✓ Good |
| D-73 (2026-08-09): arquivos que `web/api/dashboard.js` precisa (`catalog-store.js`, `schema.sql`, `admin-dashboard.js`, `catalog.db`) são **vendorizados como cópias commitadas** dentro de `web/` (`web/scripts/prepare-admin-api.mjs`, rodado manualmente/via CI, não no build da Vercel) | A Vercel não expõe arquivos fora da Root Directory do projeto durante o build — importar `../../src/...` ou ler `../../data/catalog.db` direto falha silenciosamente no deploy. `data/catalog.db` sincroniza sozinho todo dia via `daily-recompute.yml`; mudanças em `catalog-store.js`/`schema.sql`/`admin-dashboard.js` exigem re-rodar `npm run sync:admin-api` manualmente (risco de drift, não automatizado) | ✓ Funcional, drift é risco conhecido |
| D-74 (2026-08-09, retomada pós-migração de conta): deploy travado do `talgui-app-center` era **"Deployment Blocked"** por e-mail de autor de commit inválido (`danilo.prado@talgui.b`, typo faltando `com.br`) — não Deployment Protection nem problema de build | A CLI da Vercel exibia "UNKNOWN"/"Building…" indefinidamente em vez do status real "Blocked", mascarando a causa; só apareceu ao abrir o dashboard web. `git config user.email` (local+global) tinha o mesmo typo, afetando qualquer commit novo em qualquer repo até ser corrigido | ✓ Resolvido — git config corrigido, commit vazio com autoria correta (`7c3acc0`), deploy `READY` em ~3s, Basic Auth confirmado (401/401/200) |

**Bug conhecido (não bloqueante, carregado para Fase 6):** `scripts/rollback.js` tem um bug de null-pointer (CR-01, achado na revisão de código da Fase 5) — um segundo rollback consecutivo sobre o mesmo produto após um rollback que resultou em Metafield deletado (`previousValue: null`) pode travar com `TypeError`, em vez de tratar corretamente esse caso. Não afetou a verificação real desta fase (o cenário testado tinha um valor concreto, não `null`). Precisa de correção antes que rollback duplo em produto sem valor anterior seja um caminho real de uso.

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition** (via `/gsd-transition`):
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone** (via `/gsd-complete-milestone`):
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

---
*Last updated: 2026-08-10, trabalho fora do roadmap GSD — refinamento do painel administrativo do Recom CONCLUÍDO na mesma sessão (usuário decidiu não migrar de conta): bug real corrigido (card do Recom no Talgui App Center linkava para localhost:5174), os 4 cards redesenhados e verificados em produção (Card 2 mostra sem-estoque; Cards 3/4 ganharam expansão + export Excel via `exceljs`, commit `6b383ff`), persistência do Card "Cron Diário" implementada (`daily_recompute_log`, commit `74d22cd`). 293/293 testes passando. Handoff completo com todos os detalhes em `.planning/.continue-here.md`. Não altera Fase 9 (continua ADIADA) nem nenhuma decisão D-71 a D-74 anterior.*
*Anterior: 2026-08-09 (retomada pós-migração de conta), trabalho fora do roadmap GSD — painel administrativo de leitura do Recom E o hub Talgui App Center construídos e validados 100% em produção (D-71 a D-74). Fase 9 (Dashboard GA4) formalmente ADIADA para milestone futuro por decisão explícita do usuário.*
*Anterior: 2026-07-22 durante a Fase 07 (rollout no catálogo completo) — formalização da Opção B (D-61): reversão da constraint de aprovação prévia para o caminho automático, aposentamento do portão prévio do APRV-03, alteração do D-47, e registro do princípio-guia D-70. Conteúdo anterior (evolução da Fase 05) preservado.*
